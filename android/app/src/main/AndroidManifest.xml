<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 걸음수 읽기(Health Connect). 삼성헬스가 Health Connect 에 기록한 걸음수를 읽는다. -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />
    <uses-permission android:name="android.permission.health.READ_BODY_FAT" />
    <uses-permission android:name="android.permission.health.READ_LEAN_BODY_MASS" />
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE" />
    <uses-permission android:name="android.permission.health.WRITE_DISTANCE" />
    <uses-permission android:name="android.permission.health.WRITE_TOTAL_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />

    <application
        android:name=".GlobalApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme">

        <!-- 카카오 네이티브 SDK 앱키(공유 버튼 카드) -->
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/kakao_app_key" />
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 구글/카카오 OAuth 완료 후 외부 브라우저에서 앱으로 복귀 -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="health-app-five-iota.vercel.app"
                    android:pathPrefix="/auth/callback" />
            </intent-filter>

            <!-- OAuth 외부 브라우저에서 앱으로 확실히 복귀시키는 전용 콜백 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="helssu"
                    android:host="auth"
                    android:path="/callback" />
            </intent-filter>

            <!-- Health Connect 권한 사유(개인정보) 화면 — Android 14+ 통합 HC -->
            <intent-filter>
                <action android:name="androidx.health.connect.action.SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>

            <!-- 카카오톡 공유(카카오링크) 콜백 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="@string/kakaolink_host"
                    android:scheme="@string/kakao_scheme" />
            </intent-filter>
        </activity>

        <!-- Android 13 이하(별도 Health Connect 앱)용 권한 사용 화면 -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 구글 플레이 인앱결제(로드맵 7.1). 이게 없으면 결제창이 아예 안 뜬다. -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- 푸시 알림(Android 13+) — 상태표시줄 알림을 띄우려면 필요. FCM 연동 시 사용. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 실내 런닝(카메라 동작인식) — WebView getUserMedia 가 권한 프롬프트를 띄우려면 필요. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 야외 런닝(GPS) — WebView geolocation 권한 프롬프트에 필요. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 백그라운드 러닝 — 앱이 뒤로 가도 GPS 추적 유지(포그라운드 서비스 + 백그라운드 위치). -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- 내 데이터 내보내기(CSV/JSON) 저장 — Android 10(API 29)부터는 공용 다운로드 폴더에
         쓸 때 권한이 필요 없다. 그 이전 기기에서만 요청한다. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- 카메라/GPS 없는 기기도 설치 가능하게(런닝은 선택 기능). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!-- Health Connect 설치/연동 확인용 query -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <!-- 카카오톡 설치 여부 확인(공유) -->
        <package android:name="com.kakao.talk" />
        <intent>
            <action android:name="androidx.health.connect.action.SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>
</manifest>
