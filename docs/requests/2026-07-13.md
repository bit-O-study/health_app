# 요청 사항 — 2026-07-13

> 사용자(bong94688) 요청 추적. 마스터 규칙은 `CLAUDE.md`. 이 문서는 요청/진행 기록용.
> **모든 요청이 끝나면 "다 했습니다, 이 추적 문서 지워도 될까요?" 물어보고, OK 하면 삭제.**
> 브랜치: `feat/commitments-missions-and-ux-fixes`

## ✅ 완료 (2026-07-13 세션, 커밋 예정 — 사용자 OK 대기)

- [x] **버그1. 앱 초기화(백그라운드 복귀)** — 네이티브 앱에서 화면 이동·백그라운드 진입 때 현재 경로 저장, 부팅 시 홈으로 튕겼고 최근(30분) 다른 화면이면 복원(`RouteKeeper`+`route-restore.ts`). 웹 영향 없음. 단위테스트.
- [x] **버그2. 부위추가 작동 이상** — ①`/routine` 오늘부위 = 루틴 ∪ daily_plan(합집합, `todayFocuses`) ②`router.push` 뒤 `router.refresh()` 제거(push 취소 레이스가 '부위추가 이동 안 됨'의 근본원인) ③defer된 날 pin 스킵. 단위+E2E 통과.
- [x] **버그3. 운동모드 운동법 뒤로가기** — 보던 운동 rowId 를 localStorage 저장/복원(`activeRow`). 단위+E2E 통과.
- [x] **버그4. 원래 오늘 루틴 내일로 밀기** — 결정: 전체 루틴 하루 밀기(`deferRoutineOneDayAction` start_date+1, 이중밀기 가드). replace+direct 적용. 단위+E2E 통과.
- [x] **버그5. 에러 터짐** — `daily-main-editor` 카탈로그 없는 운동 undefined 방어 + 위 refresh 레이스 수정. 두 플로우 E2E 에러 0 확인.

## ✅ 추가 요청 (2차 — 운동 선택 필터링) — 완료
- [x] 1차: `ExerciseSearchSelect`에 `muscleFilter`(부위·세부근육 칩) 추가.
- [x] **2차(재확정): 부위 먼저 고르기 방식** (기존 '운동 추가' AddExerciseSlot 과 동일).
  - DailyMainEditor 행마다 **부위 드롭다운 → 그 부위 운동만** 목록(세부근육 칩은 부위 안에서 유지).
  - 직접담기 = 전체 기본 부위 선택 가능 / 전체바꾸기 = 선택 부위만 / 부위추가 = 현재+추가 부위.
  - **부위추가**: `?add=1` → /plan/today 가 현재 오늘 운동(핀된 daily_plan) + 추가 부위 함께 편집.
  - E2E: 직접담기 부위드롭다운+세부칩, 부위추가 현재+추가 검증. 회귀 4개 통과.

## 🟡 Play 스토어 준비 (코드로 한 것)
- [x] `allowBackup=false` (건강데이터 앱 권장).
- [x] `/privacy` 개인정보처리방침 페이지(회사/이메일 플레이스홀더 — **사용자 채워야 함**).
- [x] `/account-deletion` 웹 계정삭제 안내 페이지(이메일 플레이스홀더).

## 🔴 Play 스토어 — 사용자 몫(핸드오프)
- [ ] 릴리스 keystore 서명 + AAB 빌드
- [ ] Health Connect 선언 양식 + Data Safety 폼(Play 콘솔)
- [ ] /privacy·/account-deletion 의 회사명·문의 이메일 실제값 입력
- [ ] Play 콘솔에 개인정보 URL(/privacy)·데이터삭제 URL(/account-deletion) 등록

## 💬 답변 완료(코드 아님)
- NVIDIA 무료 이미지·영상 생성: **사실**(build.nvidia.com, Cosmos3=영상/Qwen-Image=이미지). SynthID 워터마크는 비가시라 사용 무방·상업사용 허용. 단 운동폼 정확도 QC 필요.
- 영상 파이프라인은 **보류**(사용자: "일단 영상 제외"). 유튜브 계정 비번은 .env 저장 안 함(OAuth 필요·유출 위험).

## 🔴 런닝 기능 개편 (2026-07-13 추가 — 큰 묶음)
- [x] 운동(/routine) 탭에 **런닝 진입 버튼** → /running (모드선택). bottom-nav 는 /running·/jog 에서 숨김.
- [x] **런닝 탭 = 실내(카메라)/야외(GPS) 2모드** (힐링 제외 — 힐링은 관리자>테스트 유지).
- [x] **실내**: 카메라 센서(제자리 달리기)로 캐릭터 이동 — 기존 유지 + **감도↑**(controls.ts).
- [x] **야외 GPS**: watchPosition → 하버사인 거리·시속·페이스(나이키런 HUD) + 속도로 캐릭터(ZenScene) 구동. geo.ts(테스트).
- [x] **끝나면 마무리 운동 자동기록**: recordRunAsCooldownAction (기존 쿨다운 보존 + 러닝 완료). 실내·야외 둘 다.
- [x] **진입 재구성**: 바깥 버튼 제거 → 운동 목록의 '런닝' 운동(itemId=running) 탭 시 /running(야외/실내 선택)으로.
- [x] **실내 정지 처리**: 감지되면 이동/안되면 정지(SPEED_MIN 0 + 애니 timeScale 0 바닥). km/높이 그대로.
- [x] **권한 안내**: 야외=위치권한 필수(거부 시 작동 안 함 명시)·실내=카메라권한 요청 문구.
- [x] **힐링모드 맵 여러 개 + 거리별 전환** — zen-scene 을 MapPreset(팔레트) 4종(초원/노을/벚꽃길/별밤)으로, 250m마다 순환 전환. 하늘/안개/조명/지면/식생 색 프리셋화(텍스처 재생성 시 dispose 유지). 야외 GPS 모드도 같은 씬이라 함께 적용. 맵 이름 HUD.
- [x] **캐릭터 교체 드롭인 준비** — character.ts(CHARACTER_MODEL_URL + pickClipName)로 모델·클립 통일. 실내/야외/힐링 모두 한 파일로 교체 가능. Mixamo 가이드 docs/RUNNING-CHARACTER.md. (실제 Mixamo GLB 넣기는 사용자 몫: 어도비 로그인+FBX→GLB)
- [~] ⚠ 야외 GPS·자동기록은 실제 폰(위치권한)에서 배포 후 검증 필요. Android WebView 는 위치권한 네이티브 설정(ACCESS_FINE_LOCATION + onGeolocationPermissionsShowPrompt) 필요할 수 있음.

## 📌 이월 (2026-07-07에서, 미완료)
- [ ] 루틴 빌더(/plan) 워밍업/본운동/마무리 박스 **통합** (일차별 1박스씩)
- [ ] **최적화 한 번 쏵** — 체감 느린 곳 집중. 사용자: "다 하고 마지막에"
- [ ] (미검증) 릴리즈는 한 번에 — 아직 push 안 함. 화면 검증은 배포/재시작 후