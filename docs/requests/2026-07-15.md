# 2026-07-15 요청 추적 (최신화)

> 폰 앱은 원격 Vercel URL 로드 → **모든 변경은 `git push` + Vercel 배포 후에야 폰에 반영**됨.
> 그룹탭 움짤 기능은 관리자 `/admin/settings → 그룹탭 모드`에서 '오늘 운동 인증(움짤)'로 전환해야 보임.
> 전체 유닛테스트 700개 green, 스키마 동기화 green, 타입체크 clean.

## ✅ 완료 (구현·테스트·커밋)

- **#1 그룹탭 움짤 인증 + 관리자 토글** — BeReal형 피드(큰 카드+인증시각), 하루 1개(교체),
  3초 무음영상 루프. 관리자 전역 모드 전환(gym↔proof). DB(group_proofs·버킷·group_mode()) 라이브 적용.
- **#2 체형기록 목표** — 온보딩에 목표 단계(체중감량/체지방감소/근육증가/유지)+목표치. profiles goal/target_* 컬럼 라이브.
  운동탭 체형기록 버튼에 지표 배지 + "N kg/% 남음"(무엇이 남았는지 명시)·탭하면 기록. (→ #20 에서 표시 개선)
- **#3 / #18 상태바·홈버튼 겹침 종합** — community 스티키 헤더, plan 이중인셋, zen-run HUD/힌트,
  운동법·메모 다이얼로그, muscle-body, 커뮤니티 댓글바(하단탭+제스처바 위로) 수정. + **safe-area 가드 테스트**.
- **#4 부위 배지 메뉴** — "등+팔" 배지 탭 → 운동 편집/오늘만 운동 바꾸기/오늘부터 다시 시작/런닝하기.
- **#5 러닝 버튼·기록·UI·권한** — 실내 레이아웃(속도·경사↑·종료↓·컴팩트), 스크롤 제거, 카메라 하늘↓길↑,
  칼로리(속도·경사·시간) 기록, 야외 GPS 오류 분기+위치설정 열기, 네이티브 HelssuNative 브릿지.
  (런닝 버튼 동작은 #19 에서 '예/아니요 대체 확인'으로 변경)
- **#6 러닝 기록 시 "오늘만" 오표시** 수정(showsDailyCooldownBadge).
- **#8 헤더 바깥 "운동 편집" 제거**(부위 배지 메뉴로 일원화).
- **#9 온보딩 여자 선택 시 배경 하얗게** 되던 것 제거(중립 배경 유지).
- **#11 workout-timer '런닝 하기' 버튼 2개 제거**(운동 시작 옆/다시 운동하기 옆) → 부위 배지 메뉴로 일원화.
- **#13 움짤 그룹원 탭 → 오늘 운동·식단 바텀시트**(섭취/소비+운동목록+끼니사진·식단). 모든 카드 탭 가능.
- **#14 움짤 헤더 상태바 겹침 / #15 전체 스크롤** — fixed→일반 흐름으로 전환(헤더까지 함께 스크롤 + 상단 인셋 상속).
- **#16 상대 상세 시트 하단 겹침** — safe-area 하단 여백.
- **#19 런닝 누르면 "오늘만 운동변경" 되던 것** — '오늘 런닝으로 대체할까요?' 예(운동 미루고 런닝)/아니요(기존 유지+런닝) 확인으로 해결.
- **#20 목표 표시 개선** — 지표 배지(체중/체지방/근육)+숫자로 예쁘게, 모호한 아이콘("@"로 보이던 Target) 제거.
- **#21-a 실내 카메라 감도 하향** — 작은 흔들림에 과속하지 않게(runIntensityFromBounce 재보정 + 테스트 갱신).
- **(추가) 러닝 오기록(14kcal)** — 실내 60초·야외 50m 미만이면 기록 안 함.
- **(추가) 인증 전에도 그룹원 인증 열람** — 블러/잠금 제거.
- **(추가) 움짤 녹화 미리보기·카메라 로딩** — '카메라 여는 중' 스피너 + 미리보기 안정 재생.

## 🔎 결정/확인된 것

- **#7 런닝 항목 탭 → 러닝모드**: 그대로 유지(사용자 결정).
- **#10/#12 헬스장 장소검색**: 카카오 기반이 이미 온보딩·설정 폼에 구현됨(추가 작업 X). ※ Vercel 에 `KAKAO_REST_API_KEY` 필요.
- **러닝 취소 시 기록 잔존**: 백엔드 `removeTodayRunAction` 은 마무리행+완료+운동시간을 완전 제거함(DB 트랜잭션 테스트로 확인).
  캘린더도 revalidate 추가. → 남아 보이면 화면 갱신 문제일 가능성(배포 후 재확인 요청).

## ⏳ 미완료 / 큰 작업 (별도 진행 필요)

- **#17 상태표시줄 푸시 알림** — 원인: 폰 앱이 **네이티브 FCM 없는 Capacitor WebView** 라 Web Push 미지원
  (google-services.json 없음, @capacitor/push-notifications 없음, POST_NOTIFICATIONS 권한 없음).
  → 상태바 알림을 받으려면 **Firebase 프로젝트 + 네이티브 푸시 플러그인 연동 + APK 재빌드** 필요(큰 작업). 진행 여부 확인 요망.
- **#21-b 그룹 런닝 순위 오버레이** — 실내 러닝 우측 상단에 그룹 런닝 순위(최대 4위 + 5번째=내 순위·달린 m),
  순위 역전 시 스르륵 애니메이션. → 러닝 거리(멤버별) 데이터 모델 + 실시간 조회 + 애니메이션 필요(큰 작업). 착수 예정.

## 메모
- 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.
