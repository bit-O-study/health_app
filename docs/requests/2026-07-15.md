# 2026-07-15 요청 추적

## 진행 중

### 1. 그룹탭 "오늘 운동 인증 움짤(3초)" 신규 기능 + 관리자 토글
- 그룹 인원들이 오늘 운동 인증을 3초짜리 움짤(GIF/짧은 영상)로 올리는 새 그룹탭 기능 추가
- 기존 그룹탭 기능과 새 그룹탭 기능 중 **어떤 걸 켤지 관리자 계정에서 전환** 가능하게
- **UI는 요즘 유행하는 "시간별 인증" 앱(BeReal 스타일)처럼**: 큰 카드 피드 + 인증 시각 강조,
  내가 인증해야 다른 그룹원 인증이 보임(안 하면 블러+잠금), 최신 인증 순 타임라인
- 완료 후 **런(실행)** 해서 확인
- 결정: ① 3초 무음영상 루프 ② 하루 1개(교체) ③ 전역 모드 1개
- 상태: 구현 중

ㄴ### 2. 운동탭 체형기록 — 회원가입 설문 목표 기반 "남은 양" + 클릭 기록
- 회원가입 설문의 목표에 따라 운동탭 체형기록 표시를 바꾼다:
  - 체중 감소 목표 → 목표체중 + "몇 kg 남음"
  - 체지방률 감소 목표 → "체지방률 몇 % 남음"
  - 근육량 (증가) 목표 → "근육량 몇 kg 남음"
- 그 표시를 클릭하면 체형 기록을 입력할 수 있게
- 상태: 대기

### 3. 상태바 겹침 — 전체 UI safe-area 점검
- 핸드폰 상단 시간·배터리(상태바) 영역과 UI가 겹침 → 수정
- 전체적으로 겹치는 곳 있는지 **순차적 사고로 화면마다 하나씩 테스트**
- 상태: 대기

### 4. 운동탭 부위(등+팔 등) 클릭 → 메뉴
- 운동탭에서 "등+팔" 같은 부위 표시 부분 클릭하면:
  - 운동 편집 / 오늘만 운동 바꾸기 / 오늘부터 다시 시작하기
- 이 부분 UI 안 깨지게
- 상태: 대기

### 5. 러닝 버튼 + 러닝 기록(마무리운동) + 러닝모드 UI/권한
- 오늘 운동 카드/메뉴에 **런닝 버튼** 추가 → 누르면 **오늘 운동 내일로 미루고** 마무리운동(쿨다운)에 **런닝 기록** 넣기
- 러닝 칼로리 기록:
  - 실내 런닝: **경사(기울기)+속도+시간** 기반 칼로리 계산 → 기록
  - 야외 런닝: 전체적으로 칼로리 계산 → 기록
- 러닝모드 스크롤 제거(불필요한 스크롤 고침)
- 실내 런닝: 종료 위치 ↔ 속도·경사 위치 바꾸기, 스크롤 없애기,
  하늘 영역 줄이고 길 위로 올려서 UI(종료/속도/경사)가 캐릭터·길 안 가리게
- 러닝모드 권한: 권한 안 잡히면 권한요청 알림, GPS(위치정보) 꺼져있으면 위치정보 켜라는 알림
  (※ 웹/WebView에서 OS 위치토글 자동 ON은 불가 — 감지+안내, 네이티브면 설정 딥링크 검토)
- 상태: 대기(러닝 코드 탐색 필요)

### 6. (버그) 런닝 기록하면 마무리운동에 "오늘만" 표시
- 원인: 러닝을 마무리운동에 기록하면 daily_conditioning(오늘만 오버라이드)에 저장돼 "오늘만" 배지가 뜸
- 처리: '기본 마무리 + 러닝 자동기록'뿐이면 "오늘만"으로 표시 안 함 (showsDailyCooldownBadge) → 완료

### 7. (질문) 런닝 항목 클릭하면 왜 러닝모드가 뜨나
- today-conditioning-list.tsx:381 — 컨디셔닝 항목 탭은 /conditioning/{id}(운동법)로 가는데
  running만 의도적으로 /running(러닝모드)로 분기("운동하다 바로 실행")
- **결정 필요**: 이대로 둘지 / running도 운동법 상세로 통일할지 → 사용자 확인

## 완료
- #3 상태바 겹침(부분): community 스티키 헤더 top-0→env inset, plan·plan/today 이중 인셋 제거
- #4 운동탭 "등+팔" 부위 배지 클릭 → 메뉴(운동 편집/오늘만 바꾸기/오늘부터 다시 시작)
- #5 러닝: TodayFocusMenu에 런닝하기(오늘 미루기+러닝) / 실내 레이아웃(속도·경사↑·종료↓, 컴팩트)
  / 스크롤 제거(fixed inset-0) / 카메라 하늘↓·길↑ / 야외 GPS 오류 분기+위치설정 열기 / 네이티브 HelssuNative 브릿지
  (칼로리 기록은 기존 recordRunAsCooldownAction+calories.ts로 이미 동작)
- #6 러닝 기록 시 "오늘만" 오표시 수정
- #2 체형기록 목표: 온보딩에 목표 단계(체중감량/체지방감소/근육증가/유지)+목표치 추가,
  profiles goal/target_* 컬럼(라이브 적용·schema-sync green), 운동탭 체형기록 버튼에 "목표까지 N kg/%" 표시·탭하면 기록
- #7 (결정) 런닝 항목 탭→러닝모드 유지(변경 없음)
- #8 운동탭 헤더의 바깥 "운동 편집" 버튼 제거(부위 배지 메뉴로 이동됨)
- #9 온보딩 여자 선택 시 배경 pink→다크모드 하얗게 되던 것 제거(중립 배경 유지)

## 대기
- #10 헬스장 입력 시 네이버 지도 검색→등록된 헬스장 클릭→네이버 주소 자동입력 (GymPlaceSuggestions 확인 필요)

## 결정사항 / 메모
- 그룹탭: DB(group_proofs 테이블 + group-proofs 버킷 + group_mode() 함수) 라이브 적용 완료, schema-sync green