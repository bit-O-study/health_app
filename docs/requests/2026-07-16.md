# 2026-07-16 요청 추적

> 폰 앱은 원격 Vercel URL 로드 → **모든 변경은 `git push` + Vercel 배포 후에야 폰에 반영**됨.
> (오전 요청이 문서로 정리 안 돼 있어 git 기준으로 복원해 정리함.)

## ✅ 완료 (구현·테스트·커밋)

- **운동 완료 연타 시 에러** — 서버 쓰기 직렬화(큐). `bfd0480`
- **움짤 인증 보드 UI 정리** — 내 카드 탭→카메라, 영상 X 삭제. `adce1e3`
- **오늘만 부위추가한 부위에 인라인 '운동 추가'** 가능하게. `9832474`
- **런닝 기록 헬스탭 표시 X** (운동 기록에만) + 런닝 클릭→운동법. `0040db2`
- **운동 시연 사진 오매핑 손수 교정** — 머신 로우/체스트프레스/머신컬/원암 케이블 등 20여 개를
  실제 데이터셋 슬러그로 교정(EXTRA 보다 우선). CDN 존재 확인 + PHOTO_CORRECTIONS 우선순위 테스트. `d176374`

- **캘린더에 실제 헬스장 가서 웨이트한 날 표시** — 근력 완료(exercise_completions done)한 날
  캘린더 셀 우하단 덤벨 마커. 러닝/컨디셔닝만 한 날은 안 뜸. E2E 추가. `a1dd077`
- **런닝모드 왼쪽 m ≠ 순위 오버레이 m 불일치** — 실내 HUD 가 씬 스크롤 단위를 표시하던 것 →
  runMetersPerSecond(속도×강도) 단일 소스로 통일(HUD·순위·기록 일치). `60251d0`
- **러닝 순위 한 그룹만 → 내가 속한 전체 그룹** 합산(중복 제거, 여러 그룹이면 '전체 그룹'). `60251d0`
- **(회귀 수정)** 움짤 보드 마지막 카드가 하단 탭바/제스처바에 가리던 것 — 하단 safe-area 인셋 복원. `4e41573`

## ⏳ 진행 중 / 대기

- (없음 — 오늘 접수된 요청 모두 커밋 완료. 사용자 OK 대기)

## 메모
- 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.