# 2026-07-17 요청 추적

> 폰 앱은 원격 Vercel URL 로드 → **모든 변경은 `git push` + Vercel 배포 후에야 폰에 반영**됨.

## ⏳ 진행 중 / 대기

ㅅ1. **[조사완료·설명대기] 6일 루틴 1일차 가슴인데 다른 루틴이 나옴** — `bong9468@naver.com`
   (uid 25dcd100…) DB 확인 결과: **루틴 데이터는 정상**. start_date=2026-07-17(오늘) → offset 0
   → day1 = `['chest','shoulder','arm']`, 실제 오늘 운동도 가슴(bench-press)부터 시작함.
   → "다른 루틴"으로 보이는 원인 = **오늘 daily_plan 오버라이드('오늘만 변경/부위추가')** 가 걸려
   있고 거기에 루틴 day1엔 없는 **core(plank)** + 여분 어깨(behind-the-neck-press)가 얹힘
   (07-17 11:37/11:57 KST 생성, 루틴 수정 11:05 이후). → 7번과 동일 원인.
   **사용자 확인 필요**: 화면에서 본 "다른 루틴"이 이 core/여분어깨 얹힌 상태가 맞는지.

2. **런닝이 운동(헬스탭)에 안 들어감** — 어제 `0040db2` 가 today-exercises.tsx 에서
   `r.itemId !== "running"` 로 헬스탭 마무리운동에서 러닝을 **통째로 숨김**. 사용자 의도는
   "런닝**모드** 화면에서 러닝 '기록' 표시만 빼라"였지 운동목록에서 러닝 제거가 아님.
   → 헬스탭 운동에 런닝 다시 보이게. (런닝모드의 어느 '기록' 표시를 빼는 건지 확인 필요)

3. **[규칙] 매일 질문·요청 기록** — 오늘 문서 누락했었음. 이 문서로 복구·기록. (사과함)

4. **[질문] 세트 축소 기록** — 벤치프레스 6세트 중 마지막(6번째)에서 5세트로 바꾸고 완료 누르면
   5세트만 기록되나? → 코드 확인해서 답변.

5. **버셀 모바일 속도 느림** — Vercel 배포/분석 확인해서 원인·개선점 조사.

6. **운동모드 세트 하한 잠금** — 이미 4세트 했으면, 세트취소 전에는 운동모드에서 4세트보다
   적게(총 세트수를 완료한 것보다 작게) 못 바꾸게 막기.

7. **오늘만 부위추가 편집 제한** — '오늘만 부위추가' 하면 편집에서 **추가요청한 부위만** 더 추가
   가능해야 하는데, 지금은 전체 부위를 다 추가할 수 있게 돼 있음. (1번의 core 유입 원인)

8. **오늘만 부위추가 시 기존 세트완료가 다 풀림** — '오늘만 운동추가'로 부위 추가하면 방금까지
   완료해둔 세트완료가 전부 해제됨. → pin(daily_plan 스냅샷)이 exercise_id/position 을 바꿔
   완료기록이 고아가 되는 것으로 추정. 조사 필요.

9. **[대규모] 운동 시연 영상/자세 오매핑 전수 감사** — 원암 케이블 리어델트 플라이, 머신로우,
   비하인드 넥 프레스 등 영상 자세가 실제 운동법과 다름. 모든 운동을 인터넷 검색 대조해
   영상↔운동법 불일치를 찾아내기. (어제 d176374 에서 20여개 손교정했으나 더 남음)
   → 범위가 커서 별도 패스로 진행 예정.

10. **시간초 기반 운동(플랭크 등) 기능** — 플랭크처럼 횟수가 아닌 '시간(초)'로 재는 운동은
    운동모드에서 초 타이머를 따로 보여주고, 완료 누르면 세트 완료. 시간 기반 운동 목록을
    카탈로그에서 찾아내 flag/기능 추가.

## ✅ 완료 (구현·테스트·커밋)

- **#6 운동모드 세트 하한 잠금** — 이미 완료한 세트(setsDone)보다 총 세트를 못 내리게.
  rest-logic 에 `minSelectableSets`/`clampTotalSets` 추가(단위테스트) + 세트 스크러버 min·putEdit 클램프.
- **#8 오늘만 부위추가 시 세트완료 풀림** — pin 이 완료기록 exercise_row_id 를 새 daily_plan
  행 id 로 재키잉 + today-plan-list 가 구성 바뀌면 done/order 재동기화. E2E 추가(통과).
- **#4 (답변)** 벤치 6세트 중 마지막에 5로 바꾸고 완료하면 **5세트로 기록됨**(현재 편집값 스냅샷).
  → 그래서 #6 하한 잠금이 필요. (사용자 OK 대기)
- **(부수) E2E 헬퍼·드리프트 복구** — 온보딩에 '목표' 단계 추가된 것 헬퍼 반영(모든 e2e 살림) +
  '오늘만 운동 바꾸기'가 부위배지 팝업 안으로 이동한 것 반영(배지에 data-today-focus-badge).
  → 5개 e2e 복구. **남은 3개는 /plan/today 하류 플로우 추가 드리프트(별도 처리 필요, 이월)**.

## 🧾 질문·결정 기록

- 1번: DB 진단 스크립트 `scripts/diag-routine-bong.mjs` 로 실계정 확인. 루틴/offset 정상,
  오늘만 오버라이드가 원인으로 특정됨.

## 메모
- 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.
- 어제(07-16) 문서: 접수분 전부 커밋 완료, 사용자 OK 대기 상태였음.