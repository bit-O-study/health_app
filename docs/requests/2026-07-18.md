# 2026-07-18 요청 추적

> 폰 앱은 원격 Vercel URL 로드 → **모든 변경은 `git push` + Vercel 배포 후에야 폰에 반영**됨.
> (07-17 세션이 자정 넘겨 이어짐 — 미완료분 이월.)

## ⏳ 진행 중 / 대기 (07-17에서 이월)

- **[확인대기] (1) 6일 루틴 1일차 "다른 루틴"** — 조사 완료: 루틴 정상, 원인=오늘 daily_plan
  오버라이드(오늘만 부위추가한 core/여분어깨). **#7 로 근본 원인(아무 부위나 추가) 차단됨.** 확인 대기.
- **(9) [대규모] 운동 시연 영상/자세 오매핑 전수 감사** — 인터넷 대조로 불일치 운동 찾기.

### 추가 기술부채(이월)
- **E2E 하류 드리프트 4건** — /plan/today 저장/전체바꾸기·워밍업 파생 흐름:
  `today-change-keeps-completed`(저장 버튼 안정화), `today-change-defers-to-tomorrow`,
  `today-change-flows-no-error ②`(/routine 재진입 후 배지),
  `conditioning-completion-not-leak`(부위변경 후 워밍업 파생). UI 변화 반영 필요.

## ✅ 완료

- #6 세트 하한 잠금, #8 부위추가 세트완료 유지, #4 답변, E2E 헬퍼/배지 드리프트 5건 복구.
  (커밋 `707df4b`, 자세한 내용 `docs/requests/2026-07-17.md`)
- **(2) 런닝** — 헬스탭 운동목록에 런닝 다시 표시(0040db2 필터 되돌림) + 런닝모드 종료화면의
  '기록 요약'(거리·시간·페이스, 기록됨 안내) 제거(야외·실내). '기록 안 됨' 경고는 유지.
  쿨다운 런닝 표시 E2E 통과로 검증.
- **(7) 오늘만 부위추가 편집 제한** — DailyMainEditor 에 addableFocuses 도입. add 모드에선
  '추가 요청한 부위'만 새로 담기/부위전환 가능(기존 오늘 부위는 유지, 추천도 요청 부위만 덧붙임).
  제한 검증 E2E 추가(통과). → #1 근본 원인 차단.
- **(10) 시간초 기반 운동(플랭크·사이드플랭크·할로우홀드)** — timed-exercises 순수모듈
  (isTimedExercise/holdSecondsFromReps, 단위테스트). 운동모드에 초 타이머(카운트업, 세트마다
  리셋) + 편집 스크러버 '시간(초)' + 오늘목록/자막 '초' 표기. 타이머 E2E 추가(통과).
- **(5) 버셀 모바일 속도 — 조사 완료**: 코드레벨은 이미 잘 최적화됨(three.js/recharts/3D
  전부 dynamic(ssr:false) 코드스플릿·병렬 데이터페칭 Promise.all·이미지 AVIF/WebP·주요
  라우트 loading.tsx 스켈레톤·@vercel/speed-insights,analytics 설치됨). 남은 체감지연은
  **구조적 TTFB**(51개 페이지 전부 force-dynamic = 사용자별 매 요청 SSR + Supabase
  ap-southeast-1 원거리 + 앱이 원격 Vercel URL 웹뷰 로드). **구체적 병목 특정엔 Vercel Speed
  Insights(모바일 필터) 실측 수치 필요** — 사용자에게 어느 페이지·LCP/TTFB 값인지 요청.

## 🧾 질문·결정 기록

- #2 런닝: '런닝모드 **종료 화면의 기록 요약**'을 빼는 것(운동목록엔 런닝 유지)로 확정.
- 우선순위: 세트/완료 버그(4·6·8·10) → 오늘만 부위추가(7·1) → 러닝(2) → 버셀·감사(5·9), 전부 순서대로.

## 메모
- 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.
