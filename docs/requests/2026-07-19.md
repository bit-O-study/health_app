# 2026-07-19 요청 추적

> 폰 앱은 원격 Vercel URL 로드 → **모든 변경은 `git push` + Vercel 배포 후에야 폰에 반영**됨.
> (07-17~18 세션 이어짐 — 미완료분 이월.)

## 🔴 원칙(법) — `docs/원칙.md`

사용자가 **원칙.md(법 문서)** 를 만들라 지시. 모든 요청 전에 먼저 읽고 반드시 지킬 것.
CLAUDE.md 맨 위에 "원칙 먼저 읽기" 참조 추가함. 현재 원칙:
1. 오늘만 운동 변경 ↔ 운동탭 편집의 **편집 가능한 부위 동일**.
2. **오늘만 운동은 영구 루틴에 영향 X**(daily_plan 만, routine_exercises 불변).

### ⚠ 원칙 코드 준수 확인(TODO)
- **원칙 #1 검증** — "오늘만 운동 변경" 편집 부위 vs "운동탭 편집" 부위가 실제로 동일한지 확인.
  #7(애드모드 요청부위 제한)이 #1과 충돌하는지 점검 필요.
- **원칙 #2 검증** — 기존 today-add-part-is-today-only E2E 가 routine 불변 보장(현재 OK).

## ⏳ 진행 중 / 대기 (이월)

- **(9) [대규모] 운동 시연 오매핑 전수 감사 — 진행 중** — 사용자 선택: **정밀 감사기 만들어
  전수 검토**. 슬러그의 실제 근육(free-exercise-db 메타) vs 운동 focus 비교해 '동작이 다른'
  것만 검출하는 감사기 개발 → 전수 교정. (audit-exercise-photos.mjs 확장)

## 🆕 신규 요청

- **(11) 커뮤니티/그룹 게시 개편**
  1. 커뮤니티탭에서 **그룹게시판 제거**.
  2. 글쓸 때 **'그룹원에게만 공개' 옵션** 추가(그룹이 쓰면 그룹원만 보이게).
  3. **그룹명 태그 표시**: 오운완(움짤)은 **오른쪽 위**, 운동게시판은 **왼쪽 아래**.
- **(12) 홈탭 신설** — ✅ 완료 `379517c` (홈탭+/home, 목표·내다짐·설정 이동, E2E).

- **(13/15) 체형정보 추이그래프** — ① 추이그래프가 **이상하게** 나옴(버그) 확인.
  ② 그래프를 **키·몸무게·근육량 등 종목별로 분리**해서 보여주기.
- **(14) 체형 기록을 버튼으로** — 체형정보 설정에서 체형 기록을 **버튼**으로 만들어 기록.
- **(16/17/18) 런닝 마무리 표시 정밀화 (#2 재설계)** — 규칙 확정:
  · **런닝모드 기록** = 캘린더 + 운동점수/시간에만 반영, **마무리운동 목록엔 표시 X**
    (지금은 표시돼서, 완료취소 시 사라졌다 재출현하는 고스트 발생 #17).
  · **루틴(또는 오늘만 편집)에서 추가한 마무리/워밍업 런닝** = 목록에 **표시 O**, 완료·휴식
    처리 정상, 계속 보임. (편집/삭제로 지우면 안 보이는 건 당연.)
  → 소스 구분 필요(런닝모드 recordRunAsCooldownAction vs routine_conditioning/오늘만).
    현재 #2 커밋(필터 전면 되돌림)은 과함 — 소스 기반 정밀 필터로 교체 필요.

## 🙋 사용자 확인/입력 필요

- **(1) 6일 루틴 1일차** — 폰에서 본 "다른 루틴"이 제가 찾은 '오늘만 부위추가로 얹힌
  코어/여분어깨' 상태가 맞는지 확인. (#7 배포되면 근본 차단됨)
- **(5) 버셀 모바일 속도** — 코드레벨 최적화는 양호. 실측 병목 특정 위해 Vercel Speed
  Insights 모바일 필터에서 **어느 페이지·LCP/TTFB 수치**인지 알려주시면 타깃 개선.

## ✅ 완료 (07-17~18 커밋, 푸시는 사용자)

- #6 세트 하한 잠금 `707df4b`
- #8 부위추가 세트완료 유지 `707df4b`
- #4 (답변) 마지막 세트 5로 줄여 완료→5세트 기록
- #2 런닝 운동목록 복구 + 종료화면 기록요약 제거 `1208376`
- #7 오늘만 부위추가 편집 '요청 부위만' 제한 `61e8a62`
- #10 시간초 운동(플랭크) 초 타이머 `ec931e8`
- 부수: E2E 온보딩/배지 드리프트 복구

### 기술부채(이월)
- E2E 하류 드리프트 4건(/plan/today 저장·전체바꾸기·워밍업 파생) — UI 변화 반영 필요.

## 메모
- 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.
