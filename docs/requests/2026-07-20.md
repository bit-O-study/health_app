# 2026-07-20 요청 추적

> 폰 앱은 원격 Vercel URL 로드 → **모든 변경은 `git push` + Vercel 배포 후에야 폰에 반영**됨.
> (07-19 세션에서 이월.)

## ⏳ 진행 중 / 대기 (07-19 이월)

- **(9) [대규모] 운동 시연 오매핑 전수 감사 — 진행 중** — 정밀 감사기 만들어 전수 검토
  (audit-exercise-photos.mjs 확장).
- **(13/15) 체형정보 추이그래프** — ① 추이그래프 버그 확인 ② 종목별(키·몸무게·근육량 등)
  분리 표시.
- **(14) 체형 기록을 버튼으로** — 체형정보 설정에서 체형 기록을 버튼으로 만들어 기록.

## 🙋 사용자 확인/입력 필요 (07-19 이월)

- **(1) 6일 루틴 1일차** — 폰에서 본 상태가 '오늘만 부위추가로 얹힌 코어/여분어깨' 맞는지 확인.
- **(5) 버셀 모바일 속도** — Speed Insights 모바일 필터에서 어느 페이지·LCP/TTFB 수치인지.

### 기술부채(이월)
- E2E 하류 드리프트 4건(/plan/today 저장·전체바꾸기·워밍업 파생) — UI 변화 반영 필요.

## 🆕 신규 요청

- **(25) 로컬 기동 확인** — ✅ 완료. predev 로직테스트 719개 + dev 서버 정상 기동 확인.
- **(26) 오늘만 운동 바꾸기 세부근육 선택(원칙#1)** — ✅ 완료·커밋 `f7ada8e`(이전 세션에서
  진행 중이던 작업, 이 세션에서 검증 후 커밋).
- **(27) 홈 대시보드 개편** — ✅ 완료(구현 완료, 사용자 확인 대기):
  1. 홈 히어로(누적 운동횟수·인사말·"오늘 운동 하러가기") 제거.
  2. 오늘 식단 기준 필요 운동량 원형 그래프 + 오른쪽에 탄단지 더 먹을 수 있는 양
     (`DietExerciseCard`) — 오늘 식단 기록 없으면 "기록 없음" 안내(아이콘+화살표) +
     카드 전체가 `/diet`로 이동하는 링크. 카드 높이는 기록 유무 상관없이 고정.
  3. 체형(체중 등) 목표 카드를 **맨 위로 이동** + 왼쪽 현재값/오른쪽 목표값을
     정가운데 얇은 구분선으로 반씩 나누고, 하단에 "N만큼 남았어요" 표시.
  4. GitHub 잔디 스타일 컨트리뷰션 그래프(최근 53주, 일요일 시작) — 운동한 시간만큼
     진해짐(0/20/40/60분 구간), 상단에 "N일 운동 · 최근 1년" 캡션.
  5. 오늘 날씨 기준 배경(맑음/비/눈/흐림/뇌우/안개) — Open-Meteo(무료·키 불필요),
     위치 권한 있으면 현재 위치, 없으면 서울 기본값. 다크모드는 기존 톤(#0a0a0b
     근처) 유지하며 색만 은은하게.
  - 새 파일: `src/features/home/dashboard-metrics.ts`(순수 로직) +
    `components/{diet-exercise-card,contribution-graph,weather-background}.tsx`.
  - `src/features/routine/calories.ts`에 `kcalToMinutes` 추가.
  - 단위테스트 `tests/be/logic/dashboard-metrics.test.ts`(10건) +
    E2E `tests/e2e/home-diet-exercise.spec.ts`(2건) 추가, 기존
    `home-tab.spec.ts` 갱신(제거된 히어로 단언 삭제). BE 780/780 green.
  - **⚠ 사용자 확인 필요**: 이렇게 진행하면 될지 — 특히 (a) 필요 운동량 계산 방식(오늘
    식단이 목표kcal 넘긴 만큼을 운동으로 상쇄한다고 가정, 넘긴 게 없으면 "목표 이내"),
    (b) 날씨 배경이 브라우저 위치 권한을 요청하는 점(거부해도 서울 기본값으로 정상 동작).

## 🙋 사용자 확인 필요 (완료, 확인 대기)

- **(28) 운동 시간이 갑자기 0분으로 초기화되는 버그** — ✅ 완료. 원인: 오늘만 바꾸기·
  전체바꾸기로 `daily_plan`이 일시적으로 비는 순간, 큐 길이(0)만 보고 세션을
  자동종료→저장해 진행 중이던 시간이 0으로 찍힘. `queue-filter.ts`에
  `shouldAutoEndSession` 가드 추가(전체 큐(queueItems)까지 0일 때만 진짜 완료로
  판단) + 단위테스트 4건 추가. BE 784/784 green.
- **(29) 플랭크 등 시간기록 운동 — 자동시작 대신 시작/중지/초기화 버튼** — ✅ 완료.
  `guided-workout.tsx`에 `holdRunning` state 추가, 진입 시 자동시작 제거하고
  시작/중지(재생·일시정지 아이콘) + 초기화(RotateCcw) 버튼으로 명시적 제어.
  기존 E2E `timed-exercise-hold-timer.spec.ts` 갱신(자동시작 가정 제거, 시작/중지/
  초기화 흐름 검증) — 통과 확인.
  - 커밋: `df06e1a`(로컬만, 미푸시).
  - **⚠ 사용자 확인 필요**: 이렇게 진행하면 될지(특히 버튼 배치·문구 "시작/계속/중지/
    초기화") 확인 부탁드립니다.
- **(30) 식단 카탈로그 누락 음식 보강** — "구운계란"이 없음 확인 필요. 누락된 음식들을
  더 조사해서 대량 추가해달라는 요청(`food-catalog.ts` / `food-catalog-world.ts`).

## 메모
- 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.