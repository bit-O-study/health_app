# 2026-07-23 요청 추적

> 오늘 요청 대부분은 **통합 관리자 콘솔(`C:\git\heltch\heltch-admin`)** 개편이다.
> 헬쑤앱 repo(this) 변경은 (5) 런닝 완료취소 버그 하나.
> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.

## ✅ 완료 (사용자 확인/배포 대기)

- **(1) 어드민 오늘 방문수 — 전체 도메인** — 통합 콘솔 `/admin`(바깥 대시보드)에 도메인별
  "오늘 방문수" 3카드 추가.
  - 아이큐 = `iq_admin_stats.today_visits`(기존 DB) · 위스키 = `site_visit` 오늘 카운트
    (`getLiquorVisitToday`, created_at KST 기준) · 헬쑤 = Vercel Web Analytics
    (`getHealthTodayVisits`).
  - ⚠ **헬쑤 값이 나오려면**: 헬쑤 Vercel 프로젝트에서 **Web Analytics ON** +
    heltch-admin 에 `VERCEL_TOKEN` 설정. 그 전엔 "—" + "Web Analytics 켜기 필요".
- **(2) 헬쑤 대시보드 그래프를 바깥(통합) 대시보드로 이동** — `HealthDashboard`(회원·접속
  차트)를 `/admin` 으로 옮김. `/admin/health` 는 `/admin/health/members` 로 리다이렉트.
- **(3) 왼쪽 메뉴 트리구조** — 사이드바를 사이트별 트리로(헬쑤앱 ▸ 회원정보/신고/관리자설정/
  운동영상 · 아이큐 ▸ 통계/문제해설 · 위스키 ▸ 상품·가격 관리). `nav-model.ts`+`admin-nav.tsx`
  재작성, 중복 HealthNav 탭 제거.
- **(4) 위스키 가격입력 이식** — 빠져있던 **판매처별 가격 입력/수정**을 콘솔에 추가
  (`addLiquorPriceAction`: liquor_price upsert + liquor_price_history append + liquor_url
  upsert). 목록 행 펼치면 "+ 가격 입력" 폼. (DB는 기존 liquor Supabase 그대로 사용 —
  마이그레이션 없음.) 상품 등록/정보수정/삭제/이력은 이미 있었음.
  - 테스트: `validatePriceInput`·`activeHref`·`seoulDayStartUtcIso` 단위테스트 추가.
    heltch-admin `npm test` = **47/47 green**, `tsc --noEmit` clean.
- **(5) [헬쑤앱] 런닝 완료취소 후 되살아나는 버그** — 원인: `today-conditioning-list.tsx`
  `setStatus` 가 런닝 완료취소 시 행을 **낙관적으로 목록에서 제거**했는데, 목록의 런닝
  행은 항상 '플랜 행'이라 서버엔 active 로 남아 → 새로고침 시 되살아나는 깜빡임.
  수정: 제거하지 않고 active 로만 되돌림. BE 756/756 green.

- **(6) [헬쑤앱] 다른 앱 갔다 복귀 시 '리로드하라' 대신 저절로 리로드** — 원인: 앱에
  에러 바운더리가 없어, 복귀 시 옛 청크/RSC 로드가 깨지면 React 가 에러를 바운더리에
  가두는데(→ `_pwa-register` 의 window 'error' 자동복구로는 안 잡힘) Next 기본 에러
  화면이 떠 수동 리로드하게 됨. 수정: `app/error.tsx`+`app/global-error.tsx` 추가 —
  에러 시 스스로 새로고침(30초 내 1회 가드, 그 뒤엔 '다시 시도' 버튼). `chunk-recovery.ts`
  에 `shouldAutoReload` 순수 헬퍼 추가(+단위테스트 4건), `_pwa-register` 와 30초 가드
  공유. BE 760/760 green.
  - 사용자 확인: 뜨는 건 **안드로이드 WebView 자체 네트워크 에러 페이지**였음 →
    네이티브도 처리(아래).
- **(6-네이티브) WebView 네트워크 에러 페이지 자동복구** — `MainActivity.java` 에
  `BridgeWebViewClient` 상속 클라이언트 추가: 메인 프레임 로드 실패 시 기본 에러 페이지
  대신 자동 재시도(백오프 0.8→8s, 최대 6회) + `onResume`(다른 앱 갔다 복귀) 시 실패
  상태면 자동 재로드. Java 컴파일(`compileDebugJavaWithJavac`) 통과.
  - ⚠ **네이티브 변경이라 APK 재빌드·설치 후에야 폰에 반영**됨(웹 배포로는 반영 안 됨).

## ⚠ 사용자가 직접 해야 할 것(배포/설정)
- 두 repo 각각 커밋 후 **push + Vercel 배포**.
- 헬쑤 오늘 방문수: 헬쑤 Vercel 프로젝트 **Web Analytics ON** + heltch-admin 환경변수
  `VERCEL_TOKEN` 설정(HEALTH_VERCEL_PROJECT_ID/VERCEL_TEAM_ID 는 코드 기본값 있음).
- (5) 런닝 버그는 배포 후 폰에서 "완료→완료취소→탭 왕복" 재확인.

## 메모
- heltch-admin 은 npm 설치(vercel.json installCommand=npm). pnpm 으로 돌리면 node_modules
  충돌 → `npm test`/`npx tsc` 사용.
