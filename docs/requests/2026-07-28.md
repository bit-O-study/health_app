# 2026-07-28 요청 추적

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.

## ✅ 완료 (사용자 확인 대기)

- **(1) 신고 처리에서 "정지"가 다른 조치를 막지 않게** — 두 repo(헬쑤앱 `/admin/reports`,
  통합콘솔 `/admin/health/reports`) **둘 다** 수정.
  - 원인: `suspendReportedUserAction` 이 정지 후 그 유저의 신고를 **전부 `resolved`**
    로 바꿨고, 화면은 resolved 행의 버튼을 통째로 숨겨서 → 정지 누르면 게시글/댓글
    삭제를 못 했다.
  - 수정:
    - 정지는 신고를 **닫지 않는다**. 삭제도 신고를 자동으로 닫지 않는다
      (닫는 건 '처리완료' 버튼으로 관리자가 직접).
    - 처리완료된 행도 **버튼을 계속 보여준다** — 콘텐츠가 남아 있으면 삭제 가능,
      작성자를 알면 정지 가능. '다시 열기' 버튼 추가(잘못 닫았을 때).
    - 행에 상태 뱃지: `처리완료` / `댓글(게시글) 삭제됨` / `정지 중 · ~날짜`·`영구정지`.
      (삭제 여부 = 대상 테이블 존재 확인, 정지 여부 = profiles 조회. **DDL 없음**)
    - 삭제는 `.select("id")` 로 실제 지워졌는지 확인 → RLS 로 0행이면 에러 메시지.
    - 댓글 신고 → **댓글만** 삭제(글은 유지). 종류별 테이블 분기는 원래대로.
  - **댓글 신고에 작성자 정지가 안 되던 것도 수정**: 댓글 신고가 `target_user_id` 를
    안 보내서 정지 버튼 자체가 안 떴다 → `CommunityComment`/`TeachingComment` 에
    `userId` 추가해 신고에 함께 전송(오운완 댓글·티칭 댓글 둘 다).
  - 테스트: 단위 `tests/be/logic/report-view.test.ts`(신규 9건) + E2E
    `tests/e2e/admin-report-actions.spec.ts`(정지 → 그대로 댓글 삭제 → DB 확인).
    BE 828/828 green, 새 E2E 2건 pass. 통합콘솔 `npm test` 54/54 green + tsc clean.

- **(2) 구글/카카오 가입자 이름이 안 뜸** — 이메일 가입만 `user_metadata.name` 이 있고
  소셜은 공급자마다 키가 달라(`full_name`/`user_name`/`preferred_username`/
  `kakao_account.profile.nickname`…) 프로필 `name` 이 빈 채로 남았다.
  - **초기엔 이름·닉네임 두 칸 모두** 같은 값으로 채운다(사용자 요청). 가입폼에서
    닉네임을 따로 받은 경우엔 그 닉네임이 우선. **이미 값이 있는 칸은 안 덮는다**
    (`socialProfilePatch`). 닉네임은 20자 컷(마이페이지 입력과 동일).
  - `socialNameFromMetadata()`(순수함수)로 후보 키를 전부 훑어 첫 이름을 쓴다.
    (이메일 형태 값은 이름으로 쓰지 않음, 40자 컷, 구글 given/family_name 은 성+이름)
  - 적용 지점 2곳: **온보딩 저장**(`saveProfileAction`) + **OAuth 콜백**
    (`/auth/callback` → `syncSocialProfileName()` — 이름이 비어 있을 때만 채움).
  - 테스트: `tests/be/logic/social-name.test.ts`(9건).
  - ✅ **백필 실행 완료(2026-07-28)** — ① 이름: 빈 프로필 10건 중 메타데이터에 이름이
    있는 **1건(구글 simbonggyo@gmail.com → "심봉교") 채움**. 나머지 9건은 전부
    이메일/비번 가입 계정이고 metadata 에 이름 자체가 없어(sub·email 만) 채울 값이
    없다 — 소셜 가입자는 아님. ② 닉네임: 빈 닉네임 중 메타데이터 이름이 있는
    **6건 채움**(심봉교×3·박준형·테스트×2). 사용한 SQL(재실행해도 빈 칸만 건드림) →
    ```sql
    update public.profiles p
       set name = nullif(trim(coalesce(
             u.raw_user_meta_data->>'name',
             u.raw_user_meta_data->>'full_name',
             u.raw_user_meta_data->>'user_name',
             u.raw_user_meta_data->>'preferred_username',
             u.raw_user_meta_data->'kakao_account'->'profile'->>'nickname',
             '')), '')
      from auth.users u
     where u.id = p.user_id
       and coalesce(trim(p.name), '') = ''
       and coalesce(trim(coalesce(u.raw_user_meta_data->>'name',
             u.raw_user_meta_data->>'full_name',
             u.raw_user_meta_data->>'user_name',
             u.raw_user_meta_data->>'preferred_username',
             u.raw_user_meta_data->'kakao_account'->'profile'->>'nickname','')), '') <> ''
       and coalesce(u.raw_user_meta_data->>'name', '') not like '%@%';
    ```

- **(4) 세트 완료 후 부위 추가/루틴 변경하면 완료한 세트가 사라짐** — 원인 두 가지.
  - ① 진행한 세트 수(`setsDone`)를 **행 id(rowId)** 로 localStorage 에 저장하는데,
    "오늘만 부위 추가"·계획 저장은 그 부위 행을 지우고 다시 넣어(행 UUID 새로 생김)
    3/4 세트가 0으로 리셋됐다. → 완료 판정과 같은 **(부위:운동) 키**로도 같이 저장해
    행 id 가 바뀌어도 이어받는다(`pickSetsDone`, 행 id 저장분이 우선).
  - ② `saveDailyPlanAction`(오늘 계획 저장)·`saveManualPlanAction`(루틴 편집 저장)이
    행을 통째로 교체하면서 **완료기록의 exercise_row_id 를 새 행으로 안 옮겼다**
    → 완료 취소가 안 먹고 낙관 상태와 어긋남. `remapRowIds`+`carryOverCompletions`
    로 같은 운동끼리 1:1 이어준다(부위 추가 pin 에 이미 있던 처리와 동일하게).
  - 테스트: 단위 11건(`row-remap.test.ts`), E2E `sets-survive-plan-change.spec.ts`
    (2세트 완료 → 부위 추가 → 재진입 시 "세트 3/4" 유지). **수정 전 실패, 후 통과** 확인.
  - 곁다리: 오늘만 시트 칩 라벨이 "어깨"→"어깨 전체"로 바뀐 뒤 방치돼 **깨져 있던 기존
    E2E 5개**(today-add-part-*, today-change-flows, today-exercise-muscle-filter)도 갱신.

- **(5) "오늘만 변경" 상태에서 완료 취소가 안 됨** (bong9468@naver.com 랫풀다운) —
  DB 확인 결과 그 완료기록의 `exercise_row_id` 가 **daily_plan/routine_exercises 어디에도
  없는 옛 행 id**(계획을 다시 담아 행 UUID 가 바뀜)였다. 화면은 (부위:운동) 폴백으로
  완료로 보여주는데, 취소는 행 id 로만 지워서 **0건 삭제** → 계속 완료로 남았다.
  - 수정 ①: 취소 시 행 id 로 지운 게 0건이면 같은 (부위:운동) 기록 **1건만** 골라 지운다
    (`pickCompletionToClear` — 같은 운동이 두 번 든 루틴에서 다른 완료는 안 건드림).
  - 수정 ②: 계획 저장(`saveDailyPlanAction`/`saveManualPlanAction`) 시 완료기록을
    **완료기록 기준**으로 새 행 id 에 이어준다 → 애초에 붕 뜬 기록이 안 생긴다.
  - 데이터 복구: 오늘자 dangling 완료기록 1건(그 계정 랫풀다운)을 새 행 id 로 연결 —
    **지금 앱에서 바로 완료 취소가 된다**(배포 전에도).
  - 테스트: 단위 5건(`completion-clear.test.ts`) + E2E `cancel-completion-after-replan.spec.ts`
    (옛 행 id 를 가리키는 완료를 스와이프로 취소 → DB 0건). **수정 전 실패, 후 통과** 확인.

- **(3) "오늘만 바꾸기" 팝업이 폰에서 깨져 보임** — 시트 패널에 높이 제한/스크롤이 없어
  내용(부위 칩 여러 줄)이 뷰포트보다 길면 화면 밖으로 잘렸다.
  → 오버레이 위쪽 여백 = `env(safe-area-inset-top) + 4.5rem`(상태바 + 앱 헤더 h-16)로
  두고 시트는 `max-h-full` + `overflow-y-auto overscroll-contain`, 작은 화면 패딩
  `p-4`(sm 이상 `p-6`). → 폰 시계·앱 헤더를 안 가리고, 내용이 길면 시트 안에서 스크롤.
  - 테스트: `tests/e2e/today-adjust-sheet-fits-phone.spec.ts` — 390×700 뷰포트에서
    시트가 화면 안에 들어오고, **헤더 아래에서 시작**하고, 맨 아래 버튼까지 닿는지.
    **수정 전엔 실패, 후엔 통과** 확인 + 스크린샷으로 육안 확인.

- **(6) 세부근육 필터가 실제로 안 걸리던 문제 + 편집기 부위 표시/선택지 정리**
  - 원인: 카탈로그 1351개 중 세부근육 매핑이 있는 건 일부뿐이고, 나머지는 부위 기본값
    (가슴 = 상부+중부+하부)으로 폴백 → **"가슴 상부만" 을 골라도 134개 중 127개가 통과**.
  - 수정: 이름/타깃 문구로 세부근육을 추론(`sub-muscle-infer`) — 인클라인=상부,
    디클라인·딥스=하부, 플라이·펙덱=내측 … 단서가 없으면 **부위 대표 1개**로만.
    → 가슴 상부 23개 / 상부+하부 48개로 실제로 좁혀진다.
  - **대분류(가슴·등·어깨·팔·하체·코어) → 중분류(세부근육)** 2단 구조를 단일 진입점으로
    정리(`exercise-category.ts`) + **모든 운동이 중분류 1개 이상**을 갖는지 가드 테스트.
  - 편집기 표시는 **부위명만**("가슴 상부, 가슴 하부" → "가슴"), 필터·추천에만 세부근육 사용.
  - 인라인 '오늘 루틴에 운동 추가': 부위 선택지를 **오늘 부위(추가한 것 포함)**로 제한
    (안 고른 부위가 뜨던 문제) + 운동 목록도 고른 세부근육으로 좁힘.
  - **부위만 추가하고 아무것도 안 담아도** 그 부위로 운동을 추가할 수 있게
    (`user_routines.today_added_date/blocks` 신규 컬럼 — 날짜 한정이라 다음 루틴엔 영향 없음).
    ⚠ DDL 라이브 DB 적용 완료(schema-sync green).
  - 테스트: 단위 26건(sub-muscle-infer·exercise-category·plan-blocks) + E2E 3건.

- **(7) 속도 개선 + 데이터 정합성 점검**
  - 미들웨어가 **모든 페이지 로드마다** `admins` 를 조회하던 것을 **/admin 경로에서만**
    조회하도록 변경 → 일반 페이지의 DB 왕복 1회 제거(정지 여부는 즉시 반영돼야 하므로
    그대로 매번 확인 — 캐시하지 않음). admin-ban E2E 로 정지 즉시 반영 확인.
  - **데이터 정합성 테스트 스위트 신설**(`tests/be/data-integrity.test.ts`, 읽기 전용):
    ① 오늘 완료기록이 살아있는 계획 행을 가리키는지(= 완료취소 먹는지) ② 완료 스냅샷 유무
    ③ 워밍업/마무리 완료기록 dangling ④ 알 수 없는 부위 값 ⑤ 완료기록 중복
    ⑥ 소셜 가입자 이름 미반영 ⑦ 신고 고아 레코드 — **현재 7/7 통과(정합성 이상 없음)**.
  - 그밖에 오늘만 시트 칩 라벨 변경 이후 stale 이던 E2E(`today-change-defers-to-tomorrow`)도 수정.

## ⚠ 사용자가 직접 해야 할 것
- 두 repo 각각 push + Vercel 배포(헬쑤앱 / heltch-admin).

## 결정사항 / 메모
- 신고는 이제 **자동으로 안 닫힌다**(정지·삭제 모두). 처리 끝나면 '처리완료'를 눌러야
  미처리 카운트에서 빠진다 — 조치가 서로를 막지 않게 하려면 이게 맞다고 판단.
