# 2026-08-12 요청 추적

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.

## 🔵 진행/완료 (사용자 확인 대기)

- **(1) 카카오톡 초대 링크로 들어오면 '캐릭터 키우기'가 뜬다** — 고침(확인 필요)
  - 증상: 그룹탭은 지금 **인증(proof) 모드**라 캐릭터 키우기(공유펫 헬스장)가 나오면 안 되는데,
    **카카오톡 초대 링크로 들어와 가입하면** 캐릭터 키우기 화면이 떴다.
  - **원인**: `/groups`(그룹탭)만 `getGroupMode()` 를 보고 모드별 화면을 그렸고,
    **`/groups/[id]` 상세 페이지는 모드를 안 봤다** — 무조건 `GroupBoard`(헬스장) 렌더.
    초대 수락(`join-confirm.tsx`)·그룹 생성/참여(`groups-client.tsx`) 가 전부
    `/groups/${id}` 로 보내고 있어서, 그 경로로 들어온 사람만 헬스장을 봤다.
  - **고침**:
    - `groupDetailRedirect(mode, id)` 가드 추가 — `/groups/[id]` 는 헬스장 전용이므로
      인증 모드면 `/groups?g=<id>` 로 리다이렉트. (옛 링크·딥링크까지 전부 막힌다.)
    - 그룹 진입 경로를 **정식 경로 `groupTabHref(id)` = `/groups?g=<id>`** 로 통일
      (초대 수락 · 그룹 만들기 · 초대코드 참여) — 모드가 바뀌어도 알아서 맞는 화면.
    - 초대 카드 문구를 모드에 맞췄다(`GROUP_INVITE_DESCRIPTION`) — 인증 모드인데
      "운동 랭킹대전에 함께 참여해요" 로 초대하던 것을 "오늘 운동 인증을 서로 남겨요" 로.
      카카오 공유 카드(네이티브·웹 SDK) + 링크 OG 미리보기 **3곳 모두** 반영.
    - `/groups/manage` 안내 문구도 모드별로("랭킹으로" ↔ "그룹으로").
  - 파일: `src/features/groups/group-mode.ts`, `src/app/groups/[id]/page.tsx`,
    `src/app/groups/join/[token]/page.tsx`, `src/app/groups/manage/page.tsx`,
    `src/app/groups/page.tsx`, `src/features/groups/components/join-confirm.tsx`,
    `groups-client.tsx`, `group-controls.tsx`
  - 테스트: `tests/be/logic/group-mode.test.ts` +8건(리다이렉트 가드·정식 경로·초대 문구),
    `tests/e2e/groups.spec.ts` — 현재 DB 모드를 읽어 **모드별로 시나리오 분기**.
    기존 랭킹 시나리오는 gym 모드에서만 돌고, **인증 모드용 회귀 테스트 신규**
    (초대 링크 가입 → 인증 피드 / 옛 `/groups/[id]` 딥링크도 헬스장 안 열림).
  - 검증: `tsc` 0 에러, BE 단위 **989/989 (114/114 파일) green**, 바뀐 파일 신규 ESLint 에러 0.
  - ✅ **확인 부탁**: 카톡 초대 링크로 새 계정 가입 → **인증 피드**가 뜨는지
    (캐릭터 키우기 안 뜨는지). 그리고 초대 카드 문구가 "오늘 운동 인증…" 으로 바뀌었는지.

## ⏭ 이월 (2026-08-11 에서, 사용자 확인 안 받음)

- 8/11 (1) 런닝 2개 중 하나만 완료취소 — 고쳤음, **사용자 확인 필요**.
- 8/11 (2) 팅김(크래시) 1차 완화 — **APK 재빌드 필요**(마지막 릴리스 `2026-07-28` 이라 폰에 미반영).
- 8/11 (3) 팅긴 뒤 '다시 운동하기' 시간 — 고쳤음, **사용자 확인 필요**.
- 8/11 (4) 하루치 루틴 소개(공유) — 구현 완료, **결정 대기**: 공개범위 기본값 / 무게 토글 /
  여러 일차 묶음 / 담김 알림 / 신고 버튼.
- 8/11 (5)(6) 런닝모드 기록 노출 — **결정 대기**: 거리·페이스 표시만 / 런닝 전용 줄까지 / 경로(GPS) 저장까지.
- 8/5 (1) 홈 성능(sin1 리전) — 배포 후 확인 필요. (2) 크론·알림 배치 커밋 — OK 전.
- 7/31 (1)~(6) 전부 — 사용자 OK 전.
- 기존부터 깨져 있던 E2E 3건: `real-account-login`, `routine-and-plan` 2건.

## 📝 메모

- `/groups/[id]` 는 이제 **헬스장 전용 딥링크**다. 새 화면을 붙일 때 그룹 진입 링크는
  `groupTabHref(id)`(`/groups?g=<id>`) 를 쓸 것 — 모드 판단이 한 곳(그룹탭)에만 있으면 된다.
- `src/features/groups/components/proof-member-sheet.tsx`·`proof-recorder.tsx` 에
  **기존 ESLint 에러 2건**(`react-hooks/set-state-in-effect`) 이 남아 있다. 이번 변경과 무관.