# 2026-08-20 요청 추적

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.

## 🔵 진행/완료 (사용자 확인 대기)

- **(1) main·현재 브랜치 커밋에서 Claude 기여자 제거** — 완료(푸시 필요, 확인 대기)
  - 저장소 전체(963커밋) 중 `Co-Authored-By: Claude ... <noreply@anthropic.com>` 가
    붙은 커밋은 **딱 3개 위치, 실제로는 2커밋**:
    - `80d9674` (feat/full-ui-renewal HEAD, PR #288 로 origin/main 에도 들어감)
    - `e51d7c3` (feat/main-page-routine HEAD, main 밖)
    author/committer 필드에 Claude/Anthropic 인 커밋은 **없었다**(트레일러만 문제).
  - **고침**: 히스토리 전체를 filter-branch 로 훑지 않고 **필요한 2커밋만 교체**.
    - `80d9674` → `f0667ba` : 트리 동일, 메시지에서 트레일러 줄만 제거,
      author/committer 이름·이메일·날짜 전부 원본 보존.
    - 머지커밋 `96f19fe`(origin/main HEAD) → `23a975c` : 부모만 새 커밋으로 바꿔 재작성.
      (로컬 main 은 origin/main 보다 75커밋 뒤라, 새 main 을 origin/main 기준으로 만들었다.)
    - `e51d7c3` → `48516b3` : 같은 방식(추가 요청으로 이 브랜치도 정리).
  - **검증**: main·현재 브랜치의 **최상위 트리 해시가 백업과 완전히 동일**(`git diff` 빈 결과),
    커밋 수 962 로 동일, author/committer 에 claude/anthropic 0건.
    로그에 남은 "Claude" 언급 17건은 전부 **CLAUDE.md 문서 / Claude Vision 기능 설명** 등 내용상 언급.
  - **최종 확인**: 로컬 브랜치 전체 + 백업 제외한 모든 ref 에서 Claude 트레일러 0건.
    옛 커밋을 아직 가리키는 건 **remote-tracking ref 뿐**(강제 푸시하면 따라온다).
  - **백업 브랜치**(되돌릴 때 사용): `backup/pre-claude-strip-main`(= 옛 origin/main),
    `backup/pre-claude-strip-ui`(= 옛 feat/full-ui-renewal HEAD),
    `backup/pre-claude-strip-mpr`(= 옛 feat/main-page-routine HEAD). 확인 끝나면 지우면 된다.
  - ⚠️ **사용자 할 일**: 히스토리가 바뀌었으므로 강제 푸시 필요 —
    `git push --force-with-lease origin main feat/full-ui-renewal feat/main-page-routine`

## ⏭ 이월 (2026-08-19 에서, 사용자 확인 안 받음)

- 8/19 (1) 순서 바꾼 뒤 '오늘만 부위 추가' 하면 순서 초기화 — 고쳤음, **배포 후 실기기 확인 필요**.
- 8/19 (2) 운동모드 하단 버튼 위치·크기 교체 + '세트 완료' 라벨 단순화 — 완료, **확인 필요**.
- 8/19 (4) 운동탭 '자극 부위' 서버 렌더 — 고쳤음, **확인 필요**.
- 8/12 (1) 카톡 초대 링크로 들어오면 '캐릭터 키우기' 뜨던 것 — 고쳤음, **확인 필요**.
- 8/12 (2) 런닝 팅김 2차 완화 — **배포 후 실기기 확인 필요**.
- 8/12 (3) APK — 같은 내용 APK 재커밋 여부 / `tools/_android-lib.sh:88` SDK 감지 버그. **결정 대기**.
- 8/11 (1)(3) 런닝 완료취소 · 팅긴 뒤 '다시 운동하기' 시간 — 고쳤음, **확인 필요**.
- 8/11 (4) 하루치 루틴 공유 — **결정 대기**.
- 8/11 (5)(6) 런닝모드 기록 노출 — **결정 대기**.
- 8/5 (1) 홈 성능(sin1 리전) — 배포 후 확인. (2) 크론·알림 배치 커밋 — OK 전.
- 7/31 (1)~(6) 전부 — 사용자 OK 전.
- 기존부터 깨져 있던 E2E: `real-account-login`, `routine-and-plan` 2건.

## 📝 메모

- **커밋 재작성 방법**: `git filter-branch` 로 900여 커밋을 다 훑을 필요 없다.
  문제 커밋이 브랜치 끝 근처면 `git commit-tree` 로 **그 커밋 + 그 뒤 커밋만** 새로 만들어
  `git branch -f` 로 갈아끼우는 게 빠르고 안전하다(트리 해시 비교로 내용 무변경 검증).
