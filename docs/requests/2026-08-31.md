# 2026-08-31 요청 추적

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.

## 🔵 오늘 요청

- **(1) 앱 전체 분석해서 "더 개발해야 될 것" 추리기** — 분석 완료, 아래 목록. **우선순위 결정 대기**
- **(2) 로드맵 하단 대기 항목부터 착수** — `docs/DEVELOPMENT-ROADMAP.md` **P2.3 Android 릴리스 운영**
  진행중으로 전환 후 구현 완료. **실기기 검증만 남음(ADB 기기 필요) + 사용자 OK 대기**
  - 다른 AI 가 1.2(번들·영상 최적화) 를 잡고 있어 파일 충돌 없는 맨 아래 항목을 가져감.
    (내가 만진 곳: `tools/`, `releases/`, `tests/be/logic/apk-release.test.ts`, 로드맵 P2.3 블록)

## 🛠 작업 (2) — P2.3 Android 릴리스 운영

### 만든 것
- `tools/release/apk-release.mjs` — 이름 규칙·중복판정·이력 표 **순수 로직**(fs/git 없음)
- `tools/release/release-apk.mjs` — CLI: `archive`(기본) / `verify` / `rollback` / `backfill` / `sync`
- `tests/be/logic/apk-release.test.ts` — 단위테스트 **34개 통과**
- `tools/RELEASE-CHECKLIST.md` — 빌드→설치→logcat→실기기 5개 영역→기록→롤백
- `releases/apk/history.json` · `HISTORY.md` — 배포 이력(자동 생성)

### 고친 것
- `tools/_android-lib.sh`
  - **SDK 감지 버그**(8/12 (3)): PATH 에만 `sdkmanager` 가 있으면 "확인" 으로 넘어간 뒤
    존재하지 않는 `$root/cmdline-tools/latest/bin/sdkmanager` 를 실행해 죽었다.
    이제 `sdkmanager_bin` 이 PATH 까지 훑어 실제 실행 가능한 경로를 돌려준다.
    Git Bash 가 `.bat` 에 실행비트를 안 주는 경우도 `-f` 로 함께 인식.
  - `archive_apk` 단계 추가(빌드 끝나면 자동 보관). `RELEASE_NOTE` / `SKIP_RELEASE` 지원.
  - 로그 문구가 `platform-34` 라고 적혀 있었는데 실제 설치는 android-36 — 문구 정정.
- `releases/README.md` — 새 이름 규칙·중복 정책·이력·체크리스트 링크로 갱신

### 이름 규칙 (확정)
```
releases/apk/<YYYY-MM-DD>/helssu-v<versionName>-<versionCode>-<커밋7>-<YYYYMMDD-HHmm>[-dirty][-메모].apk
예: releases/apk/2026-08-31/helssu-v1.0.3-4-d4717df-20260831-1612.apk
```
- 날짜·시각 **KST 고정**(빌드 머신 TZ 무관), 미커밋 상태로 구우면 `-dirty`
- release 빌드는 `helssu-release-...`

### 🔴 8/12 (3) 미결 질문 답 — 동일 내용 APK가 실제로 있었다
`backfill` 로 기존 18개를 sha256 비교한 결과 **5개가 바이트 단위로 완전히 동일**:

| 중복 파일 | 원본과 동일 |
|---|---|
| `2026-07-06/app-debug-...-8c070ea.apk` | `2026-07-06/...-1652a39.apk` |
| `2026-07-13/app-debug-...-b394822.apk` | `2026-07-13/...-b0b4fc4.apk` |
| `2026-07-15/app-debug-...-c5528e1.apk` | `2026-07-15/...-66b4565.apk` |
| `2026-07-28/app-debug-...-5c5d87c.apk` | `2026-07-23/...-f71de43.apk` |
| `2026-08-11/app-debug-...-5ea9aee.apk` | `2026-07-23/...-f71de43.apk` |

→ **7/23·7/28·8/11 세 빌드가 같은 바이트**다. 그 사이 커밋이 APK 에 안 들어갔다는 뜻
(웹은 원격 Vercel URL 을 로드하므로 네이티브 변경이 없으면 실제로 같을 수 있음).
앞으로는 보관 단계에서 자동으로 걸러진다. **기존 중복 파일 삭제 여부는 사용자 결정 대기.**

### 검증
- 단위테스트 34개 통과 / `eslint tools/release tests/be/logic/apk-release.test.ts` 무경고
- `bash -n tools/_android-lib.sh` 통과
- SDK 감지 3개 시나리오 실동작 확인 — 표준경로(.bat 실행비트 없음)=READY,
  PATH 에만 있음=READY(예전엔 여기서 죽음), 아무데도 없음=NOTREADY(다운로드로 진행)
- `rollback` 은 검증 기록이 없어 "후보 없음" 출력 — 의도된 동작
- ⏳ **실기기 검증은 못 함** — ADB 연결 기기 0대(1.1 과 같은 제약)

## 📊 분석 (1) — 현황

- 라우트 34개 / 기능 모듈 27개 / 소스 95k 줄 / DB 테이블 49개(RLS·정책 167개 전부 적용됨)
- 테스트: 단위 1001개 중 **998 pass / 3 fail**, E2E 스펙 96개(기존 2건 깨짐)
- 크론 2개 등록(`weekly-group-mvp`, `daily-reminders`), 1개 미등록(`workout-inactivity`)

## 🚧 추린 개발 과제 (우선순위순)

### P0 — 지금 사용자가 실제로 겪는 문제
1. **APK 탭 이동 중 팅김 (8/21 진단 완료, 수정 미착수)**
   - 렌더러 OOM + `RouteKeeper` 복원이 crash-loop 를 만든다.
   - 할 일: (a) 네이티브가 "크래시 복구 부팅" 플래그 전달 → 그 부팅엔 복원 안 함,
     (b) 3분 내 2회 사망 시 복원 완전 차단 + 안내.
2. **클라이언트 번들 다이어트 — 팅김의 근본 원인**
   - `exercise-catalog-extra.ts` **727KB**(+catalog 89KB)가 `exercise-catalog.ts` 를 통해
     **클라 컴포넌트 7곳**(plan-editor, today-plan-list, muscle-exercise-picker,
     exercise-search-select, daily-main-editor, exercise-library, equipment-method)에 실림.
   - 할 일: 카탈로그를 서버 전용 모듈 + 서버 액션 검색으로 분리하거나 `import()` 지연 로드.
   - 부수 효과: 단위테스트 3건 실패(`workout-active-row.test.ts` beforeEach 10s 타임아웃)와
     `pnpm test:unit` 314초(import 만 2770초 누적)도 같은 원인.
3. **가이드 영상 124MB 가 `public/` 에 그대로**
   - `bench-press-scapula.mp4` 48.7MB, `squat.mp4` 37.3MB, `bench-press-main-new.mp4` 26.3MB.
   - 할 일: Supabase Storage/CDN 로 이관 + 720p/H.264 압축 + `preload="none"`·포스터.

### P1 — 만들어놓고 안 돌거나 빠진 것
4. **`workout-inactivity` 크론이 `vercel.json` 에 미등록** → 코드 108줄이 한 번도 안 돌고 있음. 등록 여부 결정 필요.
5. **AI 엔드포인트 레이트리밋 없음** — `callAI` 사용처 5곳(식단 스캔·체성분 스캔·코치·기구 인식).
   무료 NVIDIA NIM 키라 쿼터 소진/남용 위험. 사용자별 일일 호출 상한 필요.
6. **에러 모니터링 없음** — Sentry 등 미도입. 프로덕션 크래시/서버액션 실패를 로그로만 추적 중.
7. **깨진 테스트 정리** — 단위 3건(위 2번) + E2E `real-account-login`, `routine-and-plan`.

### P2 — 성능·운영
8. **`/routine` 초기 로드** — 7/28 미들웨어 개선으로 왕복은 크게 줄었으나
   Speed Insights 기준 `/routine` RES 12 / LCP 7.56s 기록이 있었다. 배포본 재측정 필요.
9. **레포 비대** — `.git` 233MB, `releases/` 205MB(APK 14개 git 추적) + 영상 110MB.
   APK 보관 정책(최근 N개만 유지 / LFS / 릴리스 첨부) 결정 필요.
10. **`AGENTS.md`(미커밋)가 `docs/DEVELOPMENT-ROADMAP.md` 를 필수 문서로 참조하는데 파일이 없음.**
    로드맵 문서를 만들지, AGENTS.md 참조를 뺄지 결정 필요.

### P3 — 제품 확장 (기반은 있는데 얕은 영역)
11. 티칭 커뮤니티(`teaching`) 5파일 / 커밋먼트 5파일 / 루틴공유 5파일 — 화면은 있으나 알림·랭킹·피드 연결 얕음.
12. 그룹 챌린지(`group_challenges`) 코드 참조 3곳 — 챌린지 생성/진행 UI 확장 여지.
13. 수익화(구독·결제) 코드 전무. 도입 계획 여부 결정 필요.
14. 다국어(i18n) 없음 — 전부 한국어 하드코딩. 해외 출시 계획 있으면 지금이 분기점.

## ✅ 결정사항

- 로드맵 대기 항목은 **아래에서 위로** 가져간다(다른 AI 와 파일 충돌 회피).
- 작업 전 `docs/DEVELOPMENT-ROADMAP.md` 를 매번 다시 읽고, 수정은 해당 블록만 국소 편집한다.

## ❓ 사용자 확인 필요

1. **내용 중복 APK 5개를 지울까요?** (git 히스토리엔 남지만 워킹트리에서 약 60MB 절약)
2. 새 이름 규칙(`helssu-v1.0.3-4-d4717df-20260831-1612.apk`) 이대로 괜찮은지
3. P2.3 다음은 어디로 갈지 — 위로 올라가 **P2.2 CI/정적검사** 인지, 아니면 P0 로 점프인지

## ⏭ 이월 (2026-08-21 에서, 사용자 확인 안 받음)

- 8/21 (1) APK 탭 이동 팅김 — 진단 완료, **수정 미착수**(위 P0-1).
- 8/20 (1) main·현재 브랜치 Claude 기여자 제거 — 완료, **강제 푸시는 사용자 몫**.
  백업 브랜치 `backup/pre-claude-strip-{main,ui,mpr}` 확인 끝나면 삭제.
- 8/19 (1) 순서 바꾼 뒤 '오늘만 부위 추가' 시 순서 초기화 — 고침, **실기기 확인 필요**.
- 8/19 (2) 운동모드 하단 버튼 위치·크기 + '세트 완료' 라벨 — 완료, **확인 필요**.
- 8/19 (4) 운동탭 '자극 부위' 서버 렌더 — 고침, **확인 필요**.
- 8/12 (1) 카톡 초대 링크 진입 시 '캐릭터 키우기' 노출 — 고침, **확인 필요**.
- 8/12 (2) 런닝 팅김 2차 완화 — **배포 후 실기기 확인 필요**.
- 8/12 (3) 동일 내용 APK 재커밋 여부 / `tools/_android-lib.sh:88` SDK 감지 버그 — **결정 대기**.
- 8/11 (1)(3) 런닝 완료취소 · 팅긴 뒤 '다시 운동하기' 시간 — 고침, **확인 필요**.
- 8/11 (4) 하루치 루틴 공유 — **결정 대기**.
- 8/11 (5)(6) 런닝모드 기록 노출 — **결정 대기**.
- 8/5 (1) 홈 성능(sin1 리전) — 배포 후 확인. (2) 크론·알림 배치 커밋 — OK 전.
- 7/31 (1)~(6) 전부 — 사용자 OK 전.
- 기존부터 깨져 있던 E2E: `real-account-login`, `routine-and-plan` 2건.
