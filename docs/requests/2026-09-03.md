# 2026-09-03 요청 추적

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.

## 🔵 오늘 요청

- **(1) "운동 탭에서도 추가하거나 변경할것들있나 디자인적으로"** — 운동탭(`/routine`)
  디자인 점검. 코드 실측 기반으로 바꿀 것 목록 제시. **사용자 선택 대기(무엇부터 적용할지)**

## ⏭ 이월 (2026-09-02 에서, 사용자 확인 안 받음)

- 9/2 (1)~(15) 전부 **OK 대기** (2.2 과부하 추천 노출 · 2.3 캘린더 주간요약 ·
  6.1 건강 연동 · 7.1 결제/AI 한도 · RTDN · E2E 2건 · 로드맵 `[대기]` 34→0 ·
  실기기 검증 · 미커밋 변경분 전수 검증 · MP4 압축)
- 9/1 (1)~(6) · 8/31 (1)(2)(4)(5) · 8/21 (1) · 8/20 (1) ·
  8/19 (1)(2)(4) · 8/12 (1)(2)(3) · 8/11 (1)(3)(4)(5)(6) · 8/5 (1)(2) · 7/31 (1)~(6)
  — **확인/결정 대기**

## 🛠 작업 (1) — 운동탭 디자인 점검 (실측)

`/routine` + `src/features/routine/components/*` 를 훑어 **리뉴얼 토큰(cc49a56)이
안 닿은 곳**과 **정보 구조상 빠진 것**을 정리. 결론은 대화로 전달, 적용은 사용자 선택 후.

### 측정값
| 항목 | 수치 |
|---|---|
| `.app-card` 사용 | 운동탭 전체에서 **2회**(today-goal-card 뿐) |
| `zinc-*` 하드코딩 | `src/features/routine` **929회** (식단 232 · 그룹 211 · 커뮤니티 122 · 홈 32) |
| 카드 표면 | 대부분 `bg-white dark:bg-zinc-800` — 토큰 `--surface`(다크 `#16201a` 초록끼) 와 불일치 |
| 남은 푸터 | `/routine` **한 곳만** `<footer>` 보유(page.tsx:183) |
| 진행률 표시 | 없음(칼로리 kcal 만) |
| `운동 시작` CTA | h-10 알약, `편집하기`·`기구 스캔` 과 같은 줄·같은 크기 |

## 🛠 작업 (2) — 운동탭 디자인 개편 10건 (사용자 "전부 다 해줘")

로직·데이터 변경 없음(진행률 집계만 신규). 시각 언어를 리뉴얼 토큰으로 모았다.

| # | 바꾼 것 | 어디 |
|---|---|---|
| 1 | 카드 표면 `bg-white/dark:bg-zinc-800`(무채색) → 토큰 `.app-card`/`.app-surface`/`.app-field` | 운동탭 전 컴포넌트 |
| 2 | 모서리 3종(20/16/12px) → 최상위 카드 20px · 중첩 12px 두 단계로 | 행·셀·모달 |
| 3 | 부위 파스텔 히어로 바탕 → 중립 표면(색은 배지·점에만) | `data.ts TONE_STYLES` |
| 4 | `운동 시작` 을 폭 전체 h-14 주버튼으로 (부행동과 같은 줄·같은 크기였음) | `workout-session-timer` |
| 5 | **오늘 진행률 신규** — `3/8 완료` + 완료/넘김 2구간 막대, 칼로리는 한 줄로 강등 | `today-progress.ts`(신규) |
| 6 | `/routine` 에만 남아 있던 푸터 삭제 | `routine/page.tsx` |
| 7 | 제목 위계 통일 — h2(아이콘칩+굵게)/h3(같은 모양 한 단계 작게), 중복 'Today' 라벨 제거 | 운동탭 전체 |
| 8 | 스와이프 안내 장문 → 칩 3개 + **닫으면 다시 안 뜸**(기기 localStorage) | `swipe-hint.tsx`(신규) |
| 9 | 이번 주 요약 **간단형**을 7일 그리드 위에 (홈·캘린더와 같은 카드·같은 집계) | `weekly-report-card` `compact` |
| 10 | 잔글씨 하한 — `text-[10px]` → `text-[11px]` 일괄 | 운동탭 전체 |

### 🔴 왕복을 안 늘렸다
(9)의 주간 집계는 `/routine` 첫 `Promise.all` 에 **같이** 실었다. 뒤에 따로 부르면
서울↔싱가포르 왕복이 한 파 더 쌓인다.

### 🔴 '운동 시작' 을 스티키로 안 만든 이유
화면 아래 고정이면 스크롤과 무관하게 늘 보이지만, E2E 20여 스펙이 목록 하단의 행·버튼을
클릭한다 — 고정 바가 그 위를 덮으면 클릭이 바에 먹힌다. 흐름 안(진행 카드 바로 아래)
폭 전체 버튼으로 크기만 키웠다.

### 검증
- 단위 `today-progress.test.ts` **7개** 신규 → `pnpm test:unit` green
- `tsc` 무오류 / 린트 기준선 27 → **22**(새 에러 0)
- E2E **16개 통과**(5.7분) — `smoke`·`done-excluded-from-guide`·`guided-skip-reflects`·
  `workout-reorder`·`today-adjust-sheet-fits-phone`·`warmup-complete-one-only`·
  `weekly-report`(6개)·`overload-in-workout`(4개)

### 🔧 곁다리로 고친 것 — 빌드 게이트를 막던 단위테스트
`plan-editor-arm-swap-ui.test.ts` 가 `Missing NEXT_PUBLIC_SUPABASE_URL` 로 실패해
`pnpm dev`/`pnpm build` 의 pre-테스트 게이트를 막고 있었다(내 변경 전 clean tree 에서도
동일 재현 — 원인은 PlanEditor 가 서버 액션 `overload-actions` 를 import 하는데 node 테스트
환경엔 env 가 없어서). 그 액션을 mock 으로 갈아끼워 해결.

### 🔎 확인한 것 — Turbopack CSS 캐시
`.app-surface`/`.app-field` 를 넣은 뒤에도 dev 가 내려주는 CSS 의 `@layer components` 가
**옛 내용**이었다(같은 파일의 `.app-card` 는 있는데 새 규칙만 없음). Tailwind CLI 로 직접
컴파일하면 두 클래스가 정상 출력 → **툴 캐시 문제**로 확인, `.next` 비우고 재확인.
