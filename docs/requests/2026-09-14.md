# 2026-09-14 요청 추적

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.

## 🔵 오늘 요청

- **(1) "앱에서 뒤로가기 누르면 전 화면으로 돌아가야 되는데 안 돼"** — 완료(사용자 OK 대기)
  - 원인: Capacitor 코어는 안드로이드 뒤로가기를 처리하지 않는다(`@capacitor/app` 플러그인 몫인데
    미설치). 그래서 기본 동작대로 **앱이 닫혔다.**
  - `MainActivity.java` 에 `OnBackPressedCallback` 등록 — WebView 에 이전 화면이 있으면 `goBack()`,
    첫 화면이면 기본 동작(앱 닫기). 운동모드의 뒤로가기 막기(`popstate`)는 그대로 동작.
  - 테스트: `tests/be/logic/android-back-button.test.ts`.
  - 🔴 **네이티브 코드라 새 APK 빌드·설치해야 반영된다**(웹 배포만으로는 안 바뀜). 실기기 확인은 사용자.
- **(2) "버전 올리고 debug APK 빌드해줘"** — 완료
  - `versionCode 4 → 5`, `versionName 1.0.3 → 1.0.4`.
  - `android/app/build/outputs/apk/debug/app-debug.apk` 빌드(뒤로가기 수정 포함). 설치·실기기 확인은 사용자.

- **(3) "모달 떠 있을 때 뒤로가기 누르면 모달만 닫히게"** — 완료(사용자 OK 대기)
  - 공통 훅 `useBackClose(open, onClose)` (`src/lib/platform/use-back-close.ts`) +
    순수 로직 `modal-history.ts`. 모달이 열리면 히스토리 항목을 하나 쌓고(URL 그대로),
    뒤로가기(popstate)가 오면 **맨 위 모달 하나만** 닫는다. X·배경으로 닫으면 쌓은 항목을 빼 준다.
  - 🔴 웹 코드라 **APK 재빌드 불필요**(1.0.4 APK 의 WebView goBack 이 popstate 를 낸다). 웹 배포만 하면 된다.
  - 적용: ConfirmDialog(공용) + 모달/시트 23개 파일(커뮤니티 글쓰기·루틴 추천·신고, 식단 날짜/끼니/음식 추가,
    운동모드 자세분석·운동법·메모·근육 보기·티칭 업로드, 오늘 운동 메뉴·조정·목표 기록·메모, 탈퇴,
    기구 스캔, 인증 녹화·멤버 시트, 생리 기록, 운동 검색 선택 등).
  - 제외: 스플래시·날씨 배경·러닝 게임 전체화면·운동모드 본 화면(원래 뒤로가기로 안 꺼지게 막아 둠)·작은 드롭다운.
  - Next 는 외부 pushState 에 `__NA` 를 복사해 줘서 새로고침이 나지 않음을 소스로 확인.
  - 테스트: 단위 `modal-history.test.ts`(겹친 모달·StrictMode 재마운트·모달 안 화면이동·운동모드 무한루프 방지 +
    적용 파일 가드), E2E `back-close-modal.spec.ts`(모달만 닫힘·새로고침 없음·겹친 모달 한 겹씩·X 후 뒤로가기 정상).

- **(4) "새로 만든 운동 영상 누끼 따나? 플랜핏처럼 배경 같게"** — 답변·결정 완료
  - 확인 결과: 누끼 안 땀. 배경이 AI 원본마다 제각각(헬스장 실사/회색 스튜디오)이고,
    정사각 contain 여백을 고정 회색 `#b6b6b6` 으로 채워 좌우 띠가 보임(`manage-motion-guides.mjs:57`).
  - 결정: **B안 — 보간 전 포즈 패널 단계에서 누끼 → 흰색 배경 합성 → 보간**. 배경색 **흰색**.
- **(5) "B로, 흰 배경으로 시범 1개"** — 🟡 진행 중
  - 대상: `meadows-row-2`(헬스장 배경·검은 랙/원판이 겹치는 어려운 사례). 스크래치패드에서만 제작, `public/` 미변경.
  - 사용자 OK 후 도구(`manage-motion-guides.mjs`)에 반영하고 기존 통과 101종 재렌더·재검토.
  - 사용자 확인: **"운동모드 디자인" 기준** — 영상이 운동모드 화면에 녹아들어야 함(플랜핏처럼).
    운동모드 배경 `bg-zinc-50`(#fafafa, 다크 `zinc-950`) ↔ 영상 컨테이너 `bg-black`+테두리+라운드(`media-embed.tsx:123`).
    → 영상 배경을 #fafafa 로 맞추고, 운동모드의 v3 영상은 검은 박스·테두리 제거. 다크모드 처리 결정 대기.
  - 시범 완료(사용자 OK 대기): rembg `birefnet-general`(스크래치패드 venv)로 패널 8장 누끼 → 8장 공통 bbox로 크롭·확대
    → #fafafa 합성 → 기존 보간 필터 그대로. 140,304 bytes(기존 210,936). 배경·바벨·거치대 누끼 양호.
  - 남은 문제: 빠른 구간 보간 잔상(기존에도 있음, 흰 배경에서 더 보임), 거치대가 원본 패널 끝에서 잘림, 그림자 없음.
  - 결정 대기: 도구 반영 후 101종 재렌더 여부.
- **(6) "다크도 따로 만들어줘"** — 시범 완료(사용자 OK 대기)
  - 같은 누끼 PNG 로 배경 #09090b(`dark:bg-zinc-950`) 버전 렌더. 103,990 bytes. 경계 흰 테두리(halo) 없음.
  - 문제: 검은 원판·반바지·신발·거치대가 배경에 묻힘. 대응(그대로/윤곽광/원본 색 조정) 결정 대기.
  - 앱 연결 계획: `ID.mp4` + `ID-dark.mp4`, `media-embed.tsx` 가 테마로 선택, 누끼 영상은 검은 박스 제거.
- **(7) "2번 윤곽광 넣고 101종 다시 만들어줘"** — 🟡 진행 중
  - 윤곽광 시범 완료: 실루엣 바깥 2~3px(MaxFilter5+Blur1.6−alpha), #d4d4d8 45%. 검은 원판·신발·거치대 분리 확인. 108,053 bytes.
  - 🔴 **충돌 확인**: Codex 세션 2개가 같은 파이프라인 실행 중(23:09 에 reviews/verification.json 기록,
    `manage-motion-guides.mjs` 미커밋 renderVersion 4·CRF22). 도구 수정·재렌더는 사용자 확인 후.
  - 선행(충돌 없음): 통과 101종 원본 패널 누끼를 스크래치패드에 미리 추출(프로젝트는 읽기만).
  - 사용자 결정: **Codex 와 병행**(공유 JSON 은 쓰기 직전 재읽기로 충돌 최소화).
  - 도구 반영: `motion-cutout.py`·`requirements-cutout.txt`·`.venv-cutout`(gitignore), `manage-motion-guides.mjs` renderVersion 5
    (누끼 기본·라이트/다크 두 벌·`manifest-dark.json`·verification 쓰기 직전 재읽기), `guide-review.mjs` 다크 해시 일치 필수,
    재렌더 드라이버 `rebuild-cutout-guides.mjs`. 테스트: node 4개 통과(다크 해시 규칙 추가, 픽스처 `cutout:false`).
  - 앱 반영: `ExerciseMedia.darkUrl`, `motion-variant.ts`(테마별 src, 테마 모르면 로드 보류), `use-is-dark.ts`,
    `MediaEmbed` 누끼 영상은 박스·테두리 제거, 운동모드 연결. vitest 10개 통과, tsc 통과, eslint 오류 0.
    운동 상세 페이지(`exercises/[slug]`)는 흰 카드(bg-white/zinc-800) 안이라 배경색이 안 맞아 기존 박스 유지(darkUrl 미전달).
  - meadows-row-2 도구 경로 1종 확인: 243초, 라이트 288,758 / 다크 244,230 bytes(Codex 의 CRF22 적용).
  - 🟡 101종 재렌더 백그라운드 실행(약 7시간 예상). 끝나면 라이트·다크 접촉시트 재검토 → passed 기록.
- **(8) "GPU로 돌려서 빠르게 해줘"** — ❌ 불가 확인, CPU 계속
  - RTX 3050 4GB Laptop, 드라이버 537.13(CUDA 12.2).
  - onnxruntime-gpu 1.30(CUDA 13): cudaSetDevice 실패 801(드라이버보다 새 런타임).
  - onnxruntime-directml 1.24.4: birefnet-general·birefnet-general-lite 둘 다 8007000E(GPU 메모리 부족).
  - onnxruntime-gpu 1.22.0(CUDA 12.9 휠): CUDA 세션이 CPU 로 폴백, lite 결과 없음. 1.20.2 는 CUDA DLL 별도 설치 필요.
  - 결론: 이 노트북에선 GPU 누끼 불가(드라이버 업데이트 + 4GB 한계). GPU/DML venv 삭제. `motion-cutout.py` 의 CUDA/DML 선택 코드는 없으면 CPU 라 무해하게 둠.
  - 재렌더 진행: 09-15 10:12 기준 12/104 처리(11 완료, barbell-back-squat 1 실패 — 끝난 뒤 재시도). 종목당 6~10분.
  - 09-15 19:32 재렌더 프로세스가 "시스템 메모리 부족"으로 종료됨. 22/104 처리(20 완료, 2 실패). 12:10~19:32 기록 공백(원인 미확인).
    실패 2건(barbell-back-squat, cable-rope-hammer-curl-2)은 16자세지만 단독 재실행 시 정상 → 스크립트 버그 아님.
    Codex 가 같은 도구로 cable-* 종목 빌드 중인 것 확인(동시 빌드 충돌 가능성). 종목별 `.build-lock`(pid) 추가,
    드라이버는 BUSY/실패 종목을 맨 뒤로 보내 1회 재시도.
  - 09-15 21:50: 24/104 처리 후 20:01 부터 정체. 원인 = **노트북 Modern Standby**(시스템 로그 Kernel-Power 506/507, 20:01~21:47 반복 진입).
    대기 중 누끼 프로세스 CPU 0. 전원 설정은 바꾸지 않고, 재렌더 프로세스가 끝날 때까지 `SetThreadExecutionState`
    로 유휴 절전만 막는 보조 프로세스 실행(덮개 닫기·전원 버튼 절전은 못 막음).

- **(4) "지금 화면 전체적으로 조잡한데 간결하게 디자인해서 검수보고서로 보여줘"** — 진행 중
  - 검수보고서(Artifact) 전달 — 코드 변경 없음. 실측 캡처는 로컬에 `.env.local` 없어 불가(전 페이지 500) →
    컴포넌트 클래스 그대로 재구성한 목업으로 대체.
  - 진단: 색 11계열(초록도 #087f5b/#059669 2종), 임의 글자 크기 8종(11px 228·10px 63, 최소 7px),
    모서리 8종·손카드 79 vs app-card 31, 홈 블록 최대 9(광고+권한배너 2가 맨 위), 헤더/폭 불일치,
    날씨 배경은 안 보일 가능성 높은데 위치 권한 요청, Pretendard 미로드 추정.
  - 제안: 규칙 4개(색 1+의미색 2 · 글자 12/14/16/20/28 · 카드 16px 테두리만 · 공통 헤더/폭),
    홈 9→5블록(다짐+식단 → "오늘", 주간 2카드 → "이번 주", 광고 맨 아래), 적용 5단계.
  - Artifact 링크가 사용자 브라우저에서 "Page not found" → 로컬 사본 `docs/ui-review-2026-09-14.html` 로 제공(미커밋).
  - ✅ 결정(사용자 "제안대로 진행해줘"): 광고 → 홈 맨 아래 한 줄 / 날씨 배경 제거(위치 권한 요청도 제거) /
    하단 탭 6개 유지 + 라벨 11px. → 보고서 적용 5단계 구현 진행 중.
  - [1·2단계 완료] 토큰 연결(`text-brand`·`bg-brand-soft`·`text-warn`·`text-danger`) + 초록 `#059669`→`#087f5b` 통일,
    Pretendard 실제 로드(`pretendard` 패키지 dynamic-subset), `.app-card` 16px·테두리만, `.app-page` 장식 그라데이션 제거.
    홈: 권한 넛지 한 줄(한 번에 하나, `pickNudge`) → 목표 → **오늘**(다짐+식단) → **이번 주**(요약+훈련 한 장,
    `WeeklyOverviewCard` — 운동탭도 같은 카드 1장) → 잔디 → 광고 한 줄(맨 아래). 날씨 배경·위치 권한 제거.
    하단 탭 라벨 11px 고정. 테스트: 단위 `ui-simplify`·`nudge-pick`, E2E `home-layout`(신규)·`promo-banner`·`weekly-report` 갱신.
    - 홈에서 뺀 것: 식단 기록 비율 지표(식단탭·캘린더에 기록 있음), 원형 그래프(숫자 한 줄로), 부위 무지개 막대(“볼륨 비중” 한 줄로).
    - 🔴 로컬에 `.env.local` 없어 E2E 는 못 돌림(전 페이지 500) — tsc·단위테스트·린트로 확인. 실화면 확인은 배포 후 사용자.

## ⏭ 이월 (2026-09-13 에서)

- 9/13 (4) 루틴 추천글 자동 제목 제거·제목 필수 — 커밋 `f9520eb`·`4ec3c82`. 사용자 OK 대기.
- 9/13 (1)~(3) 상태 점검·SW 캐싱·영상 캐싱 — 사용자 OK 대기.
- 그 외 9/13 파일의 🟡 진행 중 / 🟦 OK 대기 / 🔴 사용자 작업 대기 / ⚪ 결정 대기 항목 전부 이월
  (상세는 `2026-09-13.md` 참고 — 운동 영상 v3 미커밋 스트림 커밋 여부 결정 대기 포함).
