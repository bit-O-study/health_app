# 2026-09-18 요청 추적

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.
> 9/15~9/17 에는 요청 추적 문서가 없어 9/14 파일에서 바로 이월했다.

## 🔵 오늘 요청

- **(1) "계속해줘"** — 🟡 진행 중 (9/14 (7) 누끼 재렌더 작업의 연속)
  - 세션 시작 시점 상태: 누끼 재렌더 드라이버가 **이미 다른 세션(pid 20964)에서 9/18 09:04 부터 실행 중**이었다.
    실수로 두 번째 인스턴스를 띄웠다가 바로 중단했다(`.build-lock` 덕분에 파일 손상은 없음). 재렌더는 계속 원래 프로세스가 담당.
  - 한 일: **누끼 재렌더로 해시가 무효화된 기존 통과 28종을 전부 재검토**했다.
    검토 방법은 원본 JPG + 라이트/다크 32프레임 접촉시트, 의심 구간은 ffmpeg 8fps 확대 타일로 추가 확인.
  - 결과: **passed 17종** / **pending 11종**. 공개 수 71 → **89**.
    - passed: dumbbell-shoulder-press, glute-bridge, hammer-curl-2, incline-push-up, incline-dumbbell-bench-press,
      kettlebell-goblet-squat, incline-barbell-bench-press, incline-dumbbell-fly, floor-press, dumbbell-rear-delt-fly,
      ez-bar-curl-2, dumbbell-overhead-extension, high-bar-squat, hip-abduction-machine, kettlebell-deadlift,
      hammer-strength-high-row, kettlebell-row (+ 재렌더 직후 kneeling-push-up, cable 재렌더분 2종)
    - pending: ez-bar-skull-crusher, hanging-knee-raise, hip-adduction-machine, kettlebell-press, farmer-s-carry,
      decline-sit-up, kettlebell-front-squat, hanging-leg-raise-2, cable-woodchopper
  - 불합격 원인은 세 가지로 정리된다(상세는 `docs/EXERCISE-VIDEO-RESUME.md` 9/18 절):
    1. **케이블 소실** — 누끼가 도르래~손잡이 케이블선을 통째로 지운다. 웨이트 스택도 흰 유령으로 깜빡인다.
    2. **보간 잔상** — 거의 전부 8패널 원본. 유령 팔·다리·머리가 겹쳐 보인다.
    3. **가는 수평 부재 끊김** — 벤치 앞다리(ez-bar-skull-crusher), 철봉(hanging-knee-raise)이 점선처럼 끊긴다.

- **(2) 케이블 종목 `cutout:false` 적용** — 완료(사용자 확인 대기)
  - 9/14 에 이미 정해진 방침(“누끼가 기구를 망가뜨리는 종목만 `cutout:false`”)을 그대로 적용한 것이라 별도 결정 없이 진행했다.
  - 확인 후 적용 3종(재렌더·재검토까지 완료): cable-rope-hammer-curl-2(passed), face-pull-2(passed), cable-woodchopper(잔상 남아 pending).
  - 재렌더 큐 도달 전 선적용 10종(원본에 케이블/스트랩이 보이는 것을 확인함):
    lat-pulldown, lat-pulldown-2, wide-grip-lat-pulldown, low-cable-fly, straight-arm-pulldown-2,
    rope-triceps-pushdown, triceps-pushdown-2, reverse-grip-pushdown, pallof-press-2, trx-row.
    → 누끼로 헛돌릴 뻔한 약 2시간을 아꼈다.
  - 🔴 부작용(의도됨): `cutout:false` 종목은 **다크 영상이 없어** 앱에서 예전 회색 여백 + 검은 박스로 보인다.
    close-grip-lat-pulldown 등 9/14 에 이미 같은 처리를 한 6종과 동일. 되돌리려면 spec 에서 플래그만 지우면 된다.

- **(3) "8패널 원본 전부 16패널로 다시 만들어줘"** — 🔴 이미지 생성 막힘 / 나머지는 준비 완료
  - 등록된 8패널 원본 **28종**을 전수 조사해 세 갈래로 나눴다.
  - **A군 20종 — 16패널 프롬프트 작성 완료.** `tools/media/motion-guides/_repanel-16/plan.json`.
    통과본 dumbbell-shrug 의 16패널 서식을 따르고, 기존 프롬프트의 출처 기반 자세 설명을 16단계로 다시 쪼갰다.
    `motion-register` 검증 규칙(프롬프트 길이·https 출처·카탈로그 기구 일치·panels16+cycle reverse)을 미리 통과시켜 확인했다.
  - **B군 5종 — 도구 제약으로 16패널 불가.** 아래 결정 대기 항목 참고.
  - **C군 3종 — 등척성 홀드라 변환 무의미.** 제외 권장.
  - 🔴 **이 세션에서는 이미지를 만들 수 없다.** imagegen 은 Codex 세션의 도구이고
    `motion-register` 는 `~/.codex/generated_images` 아래 PNG 만 받는다.
    → imagegen 이 있는 세션에서 plan.json 의 프롬프트로 `<운동ID>.png` 20개를 만들어 주면,
    `node tools/media/repanel-16.mjs <PNG 디렉터리>` 한 줄로 등록·렌더까지 끝난다(빈 디렉터리로 동작 확인 완료).
    이후 라이트/다크 접촉시트 재검토는 제가 합니다.
  - ⚠️ A군을 한꺼번에 등록하면 현재 passed 9종이 해시 불일치로 빠져 **공개 수가 90 → 81 로 일시 하락**한다.
  - 덤: A군의 실사 스타일 4종(cable-woodchopper, decline-sit-up, hanging-leg-raise-2, kettlebell-front-squat)은
    새 프롬프트가 승인된 회색 캐릭터를 명시하므로 스타일 문제도 같이 정리된다.

## ⚪ 사용자 결정 대기

- **실사 사진 스타일 원본 6종** — 승인된 회색 캐릭터/청록 반바지가 아니다.
  cable-woodchopper, decline-sit-up, hanging-leg-raise-2, kettlebell-front-squat, farmer-s-carry, kettlebell-row.
  다시 만들지, 그대로 둘지 결정 필요. (kettlebell-row 만 렌더 품질에 문제가 없어 passed, 나머지는 어차피 pending)


- **(4) "나로 도구에 16패널 full-cycle 지원 추가해줘"** — 완료(사용자 확인 대기)
  - `manage-motion-guides.mjs`: 16패널 분기를 cycle 로 나눴다. full 은 `[0,0,0, 1..15, 15,15]` 20프레임을
    되감지 않고 2회 반복해 40프레임/8초 — reverse 와 같은 프레임 예산이라 framerate·규격 검사는 그대로다.
    `motion-register` 의 "16 panels currently require reverse cycle" 가드도 제거했다.
  - 🟢 **기존 렌더 영향 없음**: 16패널 reverse 의 order 배열이 이전과 바이트 단위로 동일함을 확인했고,
    16패널+full 스펙은 가드 때문에 지금까지 존재할 수 없었다. 그래서 renderVersion 은 올리지 않았다(불필요한 전체 재렌더 방지).
  - 테스트 추가: `manage-motion-guides.test.mjs` 의 "16-pose full cycle plays forward twice instead of rewinding".
    16칸에 밝기 램프를 넣은 픽스처를 렌더해 후반부에서도 밝기가 계속 오르는지 본다.
    같은 픽스처를 reverse 로 렌더하면 5.0s=163 → 6.5s=41 로 내려가 단언이 실패하는 것까지 확인했다(테스트가 실제로 구분함).
  - node --test 2개 통과. 앱 코드가 아니라 제작 도구라 Vitest/E2E 대상 아님.
  - 후속: plan.json 의 B군 5종을 A군으로 옮기고 full-cycle 프롬프트를 새로 썼다. **A군 20 → 25종.**

## 🟡 진행 중 (백그라운드)

- 누끼 재렌더 드라이버 `rebuild-cutout-guides.mjs` (pid 20964, 9/18 09:04 시작).
  9/18 10:21 기준 **64/104**. 종목당 7~13분이라 나머지 40종은 수 시간 더 걸린다.
  끝나는 대로 재렌더분을 순차 재검토해야 한다(재검토 전까지 manifest 에서 빠짐).

## ⏭ 이월 (2026-09-14 에서)

- 9/14 (1) 안드로이드 뒤로가기 → 이전 화면 — 커밋 완료, **실기기 확인은 사용자**(APK 1.0.4 필요).
- 9/14 (3) 모달만 닫는 뒤로가기(`useBackClose`) — 커밋 완료, 사용자 OK 대기(웹 배포만 하면 됨).
- 9/14 (4)~(7) 누끼/다크/윤곽광 방침 — 도구·앱 반영 완료, 위 (1)(2) 로 이어짐.
- 9/14 (8) GPU 누끼 — 이 노트북에선 불가 확인, CPU 계속.
- 9/13 (4) 루틴 추천글 자동 제목 제거·제목 필수 — 커밋 `f9520eb`·`4ec3c82`. 사용자 OK 대기.
- 9/13 (1)~(3) 상태 점검·SW 캐싱·영상 캐싱 — 사용자 OK 대기.
- 그 외 9/13 파일의 🟡/🟦/🔴/⚪ 항목 전부 이월 (상세는 `2026-09-13.md`).

## 📌 검증 기록

- 이번 배치는 **영상 검토 전용**이라 앱 코드 변경 없음 → 테스트 게이트 대상 없음.
- 각 종목 `motion-build` 시 ffprobe(480x480/8초/24fps) + 전체 decode 통과 기록은 `verification.json`.
- 🔴 앱 화면 E2E / 실기기 재생은 이번 배치 미실행. 전체 완료로 표시 금지.


## 🔵 오늘 요청

- **(1) 트레이너 ↔ 회원 기능: feat 브랜치, 회원 연/월/주 통계 + 통계 요약 + 회원 관리,
  운동 처방(각 회원 운동 삭제·변경 권한)** — 🟡 진행 중
  - 사용자 추가 지시: **만들기 전에 "검수보고서 + 만들 기능 정리"부터 보여줄 것.**
  - 브랜치 `feat/trainer-member-management` (main 과 같은 지점에서 출발).
  - 검수보고서: `docs/trainer-회원관리-검수보고서.md` — 현황·빈틈 9개·기능 목록 A~D·결정 4건.
  - 🔴 검수에서 찾은 가장 큰 구멍: **트레이너가 회원의 루틴 "내용"(`routine_exercises`)을
    조회할 방법이 아예 없다**(RPC 는 주 모양만 준다). 처방 기능의 전제부터 만들어야 한다.
  - ⚪ 결정 대기: 트레이너 범위(그룹장 1명 vs 다중) / 처방 축(영구 루틴 vs 오늘만) /
    무게 처방 허용 / 유료 게이팅.
  - 구현은 사용자가 계획 확정한 뒤 시작. → **이후 내용은 `2026-09-20.md` 로 이월**(결정사항·진행 상황 포함).

## ⏭ 이월 (2026-09-14 에서)

- 9/14 (1) 안드로이드 뒤로가기 → 이전 화면 (`MainActivity` `OnBackPressedCallback`) — 사용자 OK 대기(실기기 확인 필요).
- 9/14 (3) 모달 떠 있을 때 뒤로가기로 모달만 닫기 (`useBackClose`) — 사용자 OK 대기.
- 9/13 (4) 루틴 추천글 자동 제목 제거·제목 필수 (`f9520eb`·`4ec3c82`) — 사용자 OK 대기.
- 9/13 (1)~(3) 상태 점검·SW 캐싱·영상 캐싱 — 사용자 OK 대기.
- 9/13 파일의 🟡 진행 중 / 🔴 사용자 작업 대기 / ⚪ 결정 대기 항목 전부 이월
  (운동 영상 v3 미커밋 스트림 커밋 여부 결정 대기 포함 — 상세는 `2026-09-13.md`).

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688. 브랜치 `feat/ui-simplify`.

## 🔵 오늘 요청

- **(1) "계속 작업해줘"** — 완료(사용자 OK 대기)
  - 9/16 (5) 에서 인계받은 **완전 재설계 8단계** 를 끝까지 마무리했다.
  - 이어받은 상태: 설정 하위·운동 상세/등록·캘린더 날짜·커뮤니티 글·생리·그룹 상세 등
    84파일이 공통 머리글(`PageHeader`)·촘촘한 카드로 바뀐 채 **미커밋** 으로 남아 있었다.
  - 이번에 마저 한 것:
    - **늑대 키우기(`/pet`)** — 마지막으로 옛 틀(`max-w-md px-4 py-6` + 글자 뒤로 줄 + 설명 문장)이
      남아 있던 사용자 화면. 공통 머리글(`‹ 그룹`)로 바꾸고 설명 문장 삭제,
      방·상점 칸을 흰 카드 직접 그리기 → 공통 토큰(`app-card`/`app-surface`/`app-field`)으로.
    - **관리자 운동 미디어** — 혼자만 `‹ 홈`(→/routine) 뒤로 줄과 다른 제목 틀을 쓰고 있었다.
      다른 관리자 화면과 같은 한 틀로(사이드바로 오가는 대시보드라 뒤로 줄 불필요).
  - 가드 테스트(회귀 방지):
    - `src/app/**/page.tsx` **전수 검사** — 사용자 화면에 `<BackLink>` 와
      옛 `mx-auto w-full max-w-… px-6 py-10` 틀이 하나도 없어야 한다(새 화면이 옛 틀로 태어나는 것 차단).
    - 늑대 키우기 머리글·토큰, 관리자 8화면 한 틀 가드.
  - 검증: Vitest 203파일 2319개 · `tsc --noEmit` · 린트 게이트(기준선 16→13건으로 낮춤) 통과.
    모바일 E2E **29스펙 89개** 통과(머리글·뒤로·404·마이페이지·홈·설정·점수·캘린더·생리·그룹·식단·수분·구독·
    내보내기·건강연동·알림·주간·관리자·근육선택·루틴등록).
  - 🔧 곁다리로 고친 것: E2E 계정 헬퍼(`tests/e2e/helpers/account-fixture.ts`)가 Supabase 키를
    `.env.local` 에서만 읽어 **모든 계정 생성 스펙이 "supabaseUrl is required" 로 죽던** 문제.
    이 프로젝트는 키가 `.env` 에 있다(9/15 (2) 에 같은 증상이 임시 우회로 기록돼 있었다).
    이제 Next 처럼 `.env` → `.env.local` → `process.env` 순으로 합쳐 읽고, 없으면 한국어로 알려 준다.
  - 커밋 `9c2a4d1` — 영상(v3) 관련 미커밋 파일(manifest·mp4·tools·EXERCISE-VIDEO-RESUME)은
    **일부러 제외**했다(9/16 (3) 에서 `feat/exercise-video-v3-batch2` 로 따로 커밋된 작업).
  - ❓ 확인 부탁: 이제 앱의 모든 화면이 같은 머리글·같은 카드로 보입니다. 이대로 괜찮을까요?
    (관리자 화면은 사이드바 대시보드라 앱 머리글로 바꾸지 않고 자기들끼리만 통일했습니다.)

## ⏭ 이월 (2026-09-16 에서)

- 9/16 (2) **운동별 연속 동작 영상(v3)** — 🟡 진행. 115/1351 공개(8.5%).
  재제작 필요: russian-twist·mountain-climber·bicycle-crunch·cable-pull-through(원본 문제),
  reverse-crunch·donkey-calf-raise·toes-to-bar·hollow-hold·v-up(잔상/잘림 반려).
  이 폴더엔 영상 파일이 **미추적 상태로 그대로** 있다(삭제 금지 — 다른 브랜치 작업분).
  원본 이미지는 Codex 이미지 생성이 필요해 이 세션에선 새로 못 만든다.
- 9/16 (3) 영상 커밋 `b7f6ec1`(`feat/exercise-video-v3-batch2`) — 사용자 OK 대기. 실기기 재생 확인 안 함.
- 9/15 (3) 다크 완전 블랙 · (4) 배너 원상복구 · (5)(7)(8) 아이폰 스타일 1~3차 · (10) 운동 아이콘 — 사용자 OK 대기.
- 🔴 9/15 (2) 중단된 E2E 로 라이브 DB 에 `e2e_*` 계정이 남아 있을 수 있다.
  (오늘 돌린 E2E 는 teardown 이 정상 동작해 86계정 전부 삭제됨 — 옛 잔여분은 별도 확인 필요.)
- 9/14·9/13 이월분 전부 계속(상세는 `2026-09-15.md`·`2026-09-14.md`·`2026-09-13.md`).
