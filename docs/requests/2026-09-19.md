# 2026-09-19 요청 추적

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688. 브랜치 `feat/ui-simplify`.
> ⚠️ 이 폴더에서 **다른 에이전트(Codex)가 동시에 작업 중**이다(음식 검색·식단 보드·
> `app-container` 애니메이션). 커밋할 때 내 파일만 골라 담는다.

## 🔵 오늘 요청

- **(1) "계속 진행해줘"** — 🟡 진행. 8단계 커밋 뒤 남은 회귀 잡는 중.
  - 어제 커밋(`9c2a4d1`) 때 돌린 건 **29스펙(89개)** 뿐이었다. 나머지 **100스펙(216개)** 를
    돌리니 **176 통과 / 37 실패**. 병렬(`--workers=4`) 탓인가 싶어 직렬로 다시 돌렸는데
    **그대로 재현** — 진짜 회귀다. (교훈: 표현만 바꾼 변경이라도 전 스펙을 돌려야 한다.)

- **(2) "중간점검하자 런해봐 화면좀보게"** — 완료
  - dev 서버 `localhost:3000` 기동. 화면 캡처 20장(다크/라이트 × 홈·운동·식단·캘린더·
    그룹·커뮤니티·설정·마이페이지·점수·운동등록) → 스크래치패드 `shots/mid-*.png`.

- **(3) "원인 확인하고 고쳐줘"** — 🟡 진행
  - **원인 A (내 커밋 `9c2a4d1`) — 고침.** 운동탭 머리글에 넣은
    `<nav aria-label="운동 도구">` 가 운동 고르기 버튼의 `aria-label="운동"` 을 가렸다.
    접근성 이름 찾기는 **부분 일치**라 `getByLabel("운동")` 이 둘로 잡혀
    운동을 담는 스펙 12개가 한꺼번에 깨졌다. 스크린리더도 같은 문제를 겪는다.
    → 랜드마크 이름을 `트레이닝 도구` 로. 가드 `tests/be/logic/aria-labels.test.ts` 추가.
  - **원인 B (1단계 커밋 `2c75cae`) — Codex 가 먼저 고쳐 둠(미커밋).**
    `.app-container { animation: app-fade-in ... both }` 의 `both` 가 애니메이션이 끝난 뒤에도
    `transform: translateY(0)` 을 남긴다. **transform 이 남은 조상은 `position: fixed` 의
    기준이 된다** → 바텀시트·모달이 화면이 아니라 본문 안에 갇혔다.
    `오늘만 운동 바꾸기` 시트가 화면 밖으로 밀리고 `/plan/today` 이동이 안 되던 원인.
  - **원인 C — 남음.** `submuscle-no-duplicate` 가 `ul.space-y-2` · `li h3` 로 운동 목록을
    찾는데 3단계(`19430eb`)에서 마크업이 바뀌어 0개로 잡힌다. 기능은 정상, **테스트를
    새 마크업에 맞게 고쳐야 한다**(CLAUDE.md: 바뀐 로직의 기존 단언은 같이 갱신).
  - 남은 실패 재분류·수정 진행 중. 대조용 워크트리 `D:/git/health_app_ui_base`(`610cf9c`) 준비됨.

- **(4) "일단 너 작업분 현상태 커밋해줘"** — 완료. 원인 A 수정 + 가드 + 이 문서만 커밋.
  Codex 작업분(`globals.css`·`diet-board.tsx`·`ui-simplify.test.ts`·`food-serving*`)은 건드리지 않음.

## 📌 결정사항

- **라이브 DB `e2e_` 계정 정리 — 사용자 결정: "지우지 말고 두기"**(2026-09-18).
  감사 결과: 전체 51계정 중 `e2e_` 30개.
  `5b1eebaa…` 18개(9/16 중단분) · `43afe885…` 7개(9/18 중단분) · `504fb61a…` 5개(당시 실행분).

## ⏭ 이월

- 9/18 (1) 8단계 재설계 `9c2a4d1` — **사용자 OK 대기**(E2E 회귀 정리 후 재확인 필요).
- **9단계 준비됨(미적용)** — 손으로 그린 흰 표면 93곳(입력칸 30·판/버튼 63)을
  `app-field`/`app-panel` 토큰으로. 다크에서 표면색이 `#141414`(카드)·`#18181b`·`#27272a`
  세 가지로 갈리는 것을 하나로. 변환안 전수 검토 끝, 회귀 정리 후 적용.
- 9/16 (2) 운동 영상 v3 115/1351 — 🟡. 재제작 9종(Codex 원본 생성 필요).
  이 폴더의 영상 파일은 미추적으로 그대로 둔다(다른 브랜치 작업분, 삭제 금지).
- 9/16 (3) 영상 커밋 `b7f6ec1` — 사용자 OK 대기. 실기기 재생 확인 안 함.
- 9/15 (3)(4)(5)(7)(8)(10) — 사용자 OK 대기(상세는 `2026-09-15.md`).
- 🔴 라이브 DB `e2e_` 계정 25개 잔존 — 사용자가 "두기" 결정. 지우지 않는다.
- 9/14·9/13 이월분 계속.
