# 2026-09-20 요청 추적

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.
> 9/19 에는 별도 요청 문서가 없어 `2026-09-18.md` 에서 바로 이월했다.

## 🔵 오늘 요청

- **(1) "다 하고 커밋해주고 너가 만든 것 중에 커밋 안 한 것 있나 봐봐"** — 완료
  - 커밋 누락 점검 **완료**: 이 세션에서 만든 것은 전부 `e1298cf` 에 들어가 있다.
    `tools/media/repanel-16.mjs`, `motion-guides/_repanel-16/plan.json`,
    `docs/requests/2026-09-18.md`, 16패널 full-cycle 코드와 테스트.
    특히 `plan.json` 은 `motion-guides/.gitignore` 의 `*/` 규칙에 걸려 빠질 뻔한 것을
    예외를 넣어 살렸고, 접촉시트 폴더는 그대로 제외되는 것까지 확인했다.
  - 재렌더 104종 전량 완료 + 재검토 대기 0 까지 끝낸 뒤 최종 커밋했다.

- **(2) `reverse-grip-pushdown` 판정 충돌** — 사용자 결정 완료: **pending 유지**
  - Codex 세션이 로드맵에 "언더핸드 그립 보존 확인, passed" 로 기록했으나,
    같은 시각 이 세션은 "완전 측면 카메라라 손바닥 방향 판별 불가" 로 pending 을 기록했다.
  - `reviews.json` 은 마지막 기록이 이기는 구조라 pending(00:03) 이 남았다.
    원본 손 부분을 8배 확대해 재확인했지만 손바닥 방향은 여전히 판별되지 않았다.
  - 사용자 판단: **pending 유지.** Codex 가 다시 덮어쓸 수 있으므로 마무리 시점에 재확인한다.

- **(3) 내 판정이 덮어써졌는지 전수 대조** — 완료
  - 이 세션이 검토한 53종 전부 기록이 그대로 남아 있다. 덮어써진 것 없음.
  - (초반 리뷰를 영어로 쓴 탓에 한글 여부로 거르면 29건이 덮어써진 것처럼 보이는 오탐이 난다. 판정·타임스탬프로 대조해야 한다.)

- **(4) "이어서 해줘"** — 앱 화면 E2E 를 실행해 9/18 부터 🔴 로 남아 있던 검증 구멍을 메웠다.
  - **v3 전종목 재생 E2E 통과**: `demo-video-fits-phone.spec.ts` 8배치 **8/8 green**(9.1분).
    manifest 82종 + v2 대조군 4종 = **86 페이지**를 실제 앱에서 열어 `src`·디코드·재생·높이 상한까지 확인했다.
  - ⚠ **처음 4배치가 실패한 건 앱 버그가 아니라 포트 충돌이었다.** 오전 10:41 에 떠 있던 옛 dev 서버가
    3000 을 잡고 있어 내가 띄운 서버는 3001 로 밀렸는데, playwright 기본 BASE_URL 이 3000 이라
    **리뷰 갱신(11:14) 이전 상태의 서버**를 때렸다. `E2E_BASE_URL=http://localhost:3001` 로 다시 돌리니 전부 통과.
    → 앞으로 E2E 전에 **dev 서버가 실제로 몇 번 포트에 떴는지** 확인할 것.
  - **새 회귀 테스트 추가**: `tests/e2e/workout-motion-dark-video.spec.ts`.
    운동모드에서 누끼 영상이 테마별 두 벌(`ID.mp4` / `ID-dark.mp4`)로 붙는지, 480정사각 8초로 실제 디코드되는지,
    누끼 영상에 검은 박스가 안 둘러지는지를 고정한다. 그동안 **화면 캡처로만 확인하던 것**이라 회귀가 없었다.
    - 테마는 OS 설정이 아니라 `localStorage['heltch.theme']`(미설정 = 다크)로 정해지고 ThemeScript 가
      첫 페인트 전에 클래스를 붙인다 → `emulateMedia` 로는 안 바뀐다. 저장 후 재진입해야 한다.
    - 운동모드는 별도 라우트가 아니라 루틴 화면 위 오버레이라 reload 로 유지되지 않고,
      한 번 들어갔다 나오면 CTA 가 "운동 시작" → "다시 운동하기" 로 바뀐다(둘 다 받도록 작성).
    - **음성 확인 완료**: `high-bar-squat-dark.mp4` 를 잠시 치우고 돌리니 `readyState 0` 으로 실패했다
      (테스트가 실제로 구분한다). 파일은 바이트 동일하게 복원했다.
    - ⚠ 그 잠깐 사이 **다른 세션이 manifest 를 재생성해 `high-bar-squat` 가 `manifest-dark.json` 에서 빠졌다.**
      커밋본으로 되돌려 65종 원복 확인. → 공유 산출물을 임시로 옮기는 검증은 다른 세션이 놀 때만 할 것.
  - 정적 정합성도 같이 확인: 라이트 82 / 다크 65 파일 전부 디스크에 존재, `cutout:false` 종목이
    다크 manifest 에 섞인 것 없음, `reviews.json` ↔ `verification.json` **해시 불일치 0**(재검토 대기 없음).
  - 🔴 남은 것: **실기기(Android) 재생**은 여전히 미확인(APK 필요, 사용자 몫).

## ✅ 완료

- **누끼 재렌더 104종 전량 완료** (`done; failed 0`, 2026-09-20 02:10 UTC).
  재렌더로 해시가 무효화된 분량을 전부 재검토해 **재검토 대기 0**.
  최종: 등록 117 / 렌더 132 / **공개 82**(다크 65 + cutout:false 회색박스 17) / pending 23 / rejected 12 / 대상 1351.

### 누끼 파손 유형 4가지 (이번 전수 검토로 확정)

1. **케이블·스트랩 소실** — 굵기와 무관하게 무조건 지워진다. 해결: `cutout:false` (누적 16종).
2. **누운 벤치 소실** — 몸이 벤치를 가리면 rembg 가 배경으로 판정한다.
   `lying-triceps-extension`·`ez-bar-skull-crusher`·`nordic-hamstring-curl` 을 `cutout:false` 로 복구.
   (9/18 에 ez-bar-skull-crusher 를 "보간 문제, 누끼 탓 아님" 으로 적었던 것은 오진이었고 리뷰 노트에 정정했다.)
3. **가는 수평 부재 끊김** — 전부 끊기는 게 아니라 **굵기 문제**다.
   끊김: `hanging-knee-raise` 철봉. 멀쩡: 스미스 레일·평행봉·랙 기둥(구멍 패턴까지 유지).
4. **빠른 구간 보간 뭉개짐** — 누끼와 무관. 대부분 8패널 원본이라 **16패널 재생성으로만** 해결된다.

### 원본 자체가 틀린 것 2종 (렌더로는 해결 불가)

- `pallof-press-2` — 16칸 전부 손이 가슴에 붙어 있어 **프레스 구간이 아예 없다.** 정지 화면처럼 보인다.
- `reverse-grip-pushdown` — 완전 측면 카메라라 이 종목을 정의하는 **언더핸드 그립을 판별할 수 없다.**
  (Codex 세션은 passed 로 기록했으나 사용자 결정으로 pending 유지. 8배 확대 재확인에도 판별 불가.)

### 도중에 겪은 것 — 다음 세션이 알아야 할 것

- 9/18 17:53 ~ 9/19 23:41 사이 **약 23시간 정체.** 프로세스는 살아 있었는데 노트북이 잠들어 굶은 것이다.
  `keep-awake.ps1` 은 유휴 절전만 막고 **덮개 닫기는 못 막는다**(9/14 에 이미 기록된 한계).
  사용자가 노트북을 깨워 둔 뒤 정상 속도(종목당 10~15분)로 재개됐다.
  → 장시간 재렌더를 돌릴 때는 덮개를 열어 두거나 전원 설정을 따로 봐야 한다.
- `cutout:false` 선적용이 크게 절약됐다. 해당 종목은 rembg 를 건너뛰어 **50~60초**에 끝났다
  (누끼 종목은 10~30분). 9/18 에 케이블 10종을 미리 잡아둔 것이 그대로 시간으로 돌아왔다.
- **빌드 중인 종목을 검토하면 안 된다.** 라이트만 먼저 렌더된 중간 상태에서도 해시가 바뀌어
  "재검토 대기" 로 잡히므로, `.build-lock` 이 없는지 확인하고 검토해야 한다(seated-leg-curl-2 에서 실제로 겪음).

## ⚪ 사용자 결정 대기 (이월)

- **실사 사진 스타일 원본 8종** — 승인된 회색 캐릭터/청록 반바지가 아니다.
  cable-woodchopper, decline-sit-up, hanging-leg-raise-2, kettlebell-front-squat,
  farmer-s-carry, kettlebell-row, **trx-row**, **v-up-2**.
  (이 중 6종은 16패널 재생성 대상이라 새 프롬프트에서 회색 캐릭터를 명시해 자동으로 해결된다.
  나머지 `kettlebell-row`·`trx-row` 는 렌더 품질에 문제가 없어 passed 로 두었고 스타일만 리뷰 노트에 남겼다.)
- **`tools/media/imports/` (21MB) 커밋 여부** — rembg 모델 바이너리 4.4MB, 다른 프로젝트 임포트 4.9MB,
  재생성 전 백업 이미지 약 11MB. 모델 바이너리를 git 에 넣으면 되돌리기 번거로워 일단 제외했다.
- **`bash.exe.stackdump`** — 크래시 덤프. 인계 문서에 "무관한 미추적 파일" 로 적혀 있어 제외했다.

## ⏭ 이월 (2026-09-18 에서)

- 9/18 (3) 8패널 → 16패널 재생성: A군 25종 프롬프트·드라이버 준비 완료.
  **이미지 생성만 남았고 이 세션에서는 불가**(imagegen 은 Codex 세션의 도구, `motion-register` 는
  `~/.codex/generated_images` 아래 PNG 만 받는다). `node tools/media/repanel-16.mjs <PNG 디렉터리>` 한 줄로 이어진다.
- 9/18 (2) 케이블 `cutout:false` 13종 — 부작용(다크 영상 없음 → 앱에서 회색 박스)은 실제 화면 캡처로 확인함. 사용자 확인 대기.
- 9/14·9/13 의 🟡/🟦/🔴/⚪ 항목 전부 이월 (상세는 각 날짜 파일).

## 📌 검증 기록

- 커밋 `e1298cf`(영상 v3 스트림 263파일): vitest 단위 189파일 1986테스트, 스키마 가드 69테스트,
  node --test 제작 도구 2개·guide-review 3개, eslint 오류 0, `tsc --noEmit` 통과.
- 커밋 `2a5cd88`(린트 정리 14파일): 사용자 요청으로 **게이트 미실행**.
- ✅ **앱 화면 E2E 실행 완료**(위 (4)): `demo-video-fits-phone` 8/8 green — 86 페이지 실재생 확인.
  운동모드 라이트/다크 전환은 새 E2E `workout-motion-dark-video.spec.ts` 로 회귀 고정(음성 확인까지 완료).
- 🔴 실기기(Android) 재생은 여전히 미확인(APK 필요).
- 커밋 전 게이트: `pnpm test` green(198파일 2159테스트), `tsc --noEmit`·eslint 통과.

> 브랜치: `feat/trainer-member-management`

## ⚠ 이 브랜치는 **다른 세션과 동시에 작업 중**이다

같은 작업 트리에서 다른 Claude 세션이 트레이너 통계·처방을 구현 중(08:44~ 파일 기록).
그래서 이 세션은 **겹치지 않는 것만** 한다(사용자 결정, 2026-09-20).

- 그 세션 소유(건드리지 않음): `member-report.ts`·`member-report-data.ts`·
  `prescription-actions.ts`·`components/member-prescription.tsx`·
  `app/groups/[id]/trainer/members/**`·`trainer-board-view.tsx`·
  `supabase/migrations/202609200001_trainer_member_management.sql`
- 이 세션 소유: 홈 헤더 모드 전환 · 회원의 정보 제공 설정(트레이너 제거 포함)

## 🔵 오늘 요청

- **(1) 검수보고서 + 만들 기능 정리 먼저** — ✅ 완료
  - `docs/trainer-회원관리-검수보고서.md` (현황·빈틈 9개·기능 A~D·결정 4건)
- **(2) 결정사항(사용자 답변)** — 기록
  - 트레이너 범위: **그룹장 1명 유지** (역할 테이블 추가 안 함)
  - 처방 축: **영구 루틴 + 오늘만 둘 다** → 🔴 다른 세션 구현은 현재 **영구 루틴만**
    (`trainer_prescribe_exercise` 주석에 "Never edit daily_plan"). 오늘만 축은 **미구현**.
  - 무게 처방: **지정 가능하되 기본은 비움** (루틴 통째 복사는 지금처럼 무게 제외)
  - 유료 게이팅: **지금은 안 건다**
- **(3) 홈 상단 헤더에서 트레이너 모드 전환(바이낸스 지갑처럼 체크로 왔다갔다),
  일반 회원에게는 안 보이게** — ✅ 완료(사용자 OK 대기)
  - 순수 로직 `features/groups/trainer-switch.ts` + 서버 조회 `trainer-switch.server.ts`
    (**내가 그룹장인 그룹만** — 참여만 한 그룹은 제외) + 헤더 컴포넌트 `trainer-mode-switch.tsx`
  - `app/home/page.tsx` 헤더에 장착. 소유 그룹 0개면 컴포넌트가 `null` → 일반 회원에겐 없음.
  - 체크는 항상 정확히 하나(목록에 없는 그룹 주소면 '내 운동' 으로 복귀).
  - 테스트: `tests/be/logic/trainer-switch.test.ts`(10) + E2E `tests/e2e/trainer-mode-and-share.spec.ts`
  - ⏭ 남음: **트레이너 화면 쪽 복귀 스위치**(그쪽 세션이 `trainer-board-view.tsx` 를 잡고
    있어 지금 못 넣음). 그 세션 끝나면 헤더에 같은 컴포넌트를 올린다.
- **(4) 일반 회원이 설정에서 트레이너 제거 + 내 정보 제공 항목 on/off** — ✅ 완료(사용자 OK 대기)
  - DB: `member_share_prefs`(user_id, group_id, share_workout/diet/body, allow_prescription) +
    RLS(본인 쓰기·그룹장 읽기 전용) + `member_shares()` + `leave_trainer_group()`
    → `supabase/migrations/202609200002_member_share_prefs.sql`, `supabase/schema.sql`
  - 화면: `/settings/trainers` (설정 목록에 카드 추가). 트레이너별 4개 토글 + '제거'(확인 모달).
  - 제거 = 그룹 탈퇴 + 그 그룹의 트레이너 코멘트·동의 설정 삭제. **루틴은 안 지운다.**
  - 실제 적용(스위치가 장식이 되지 않게):
    - 회원 관리 보드: 끈 항목의 값을 **서버에서 지우고** `hidden` 으로 내려 '비공개' 칩 표시.
      비공개는 가중치 0(정렬 안 흔듦)·'챙길 회원' 수에서 제외.
    - 그룹원 상세(`/groups/[id]/member/[uid]`): **보는 사람이 그룹장일 때만** 가림 +
      '회원이 ○○ 제공을 꺼 뒀어요' 안내. 주간 훈련 분석도 운동 비공개면 안 줌.
    - 루틴 배정: `trainer_assign_routine_day` 에 동의 확인 추가(-1) + 액션에서 이유 안내.
  - 테스트: `tests/be/logic/share-prefs.test.ts`(13) + 위 E2E.

- **(5) 세션 분담(사용자 결정, 22:5x)** — 이 세션은 **B2B / 트레이너 화면 쪽**만 맡는다.
  - 하단 바(탭 순서·홈 가운데 고정)는 **이 세션이 건드리지 않는다.** 논의만 하고 미적용.
    (논의된 안: `[운동] [식단] [캘린더] [홈] [그룹] [커뮤니티]` — 다른 세션/추후 처리)
- **(6) 트레이너 화면 '내 운동' 복귀 스위치** — ✅ 완료(사용자 OK 대기)
  - `/groups/[id]/trainer` 상단에 홈 헤더와 **같은** `TrainerModeSwitch` 를 올렸다
    (체크 위치가 두 화면에서 어긋나지 않게 같은 컴포넌트·같은 로직).
- **(7) 깨져 있던 `pnpm test` 복구** — ✅ 완료
  - `trainer/members/[memberId]/loading.tsx` 가 공통 `RouteLoading` 을 안 써서 red 였다.
- **(8) '오늘만' 처방 축 구현** — ✅ 완료(사용자 OK 대기)
  - DB `supabase/migrations/202609200003_trainer_today_prescription.sql` + `schema.sql`
    - `valid_prescription_patch(jsonb)` — 두 축이 **같은 검증**을 쓰도록 분리
      (`trainer_prescribe_exercise` 도 이걸 부르게 바꿈, 동작 동일)
    - `trainer_member_today_plan(group, member)` — 회원의 오늘 운동
      (daily_plan 오버라이드 있으면 그것, 없으면 루틴의 오늘 일차)
    - `trainer_prescribe_today(...)` — **daily_plan 에만** 쓴다(원칙 #2)
  - 🔴 오늘만 오버라이드는 **부위 단위 통째 교체**라, 한 줄만 고쳐도 그 부위 전체를
    루틴에서 복사해 고정한다(안 그러면 나머지 운동이 오늘 화면에서 사라진다).
    화면 확인 단계에 "○○ 운동 전체가 오늘 하루치로 고정됩니다" 안내를 넣었다.
  - 🔴 **휴식일 / 회원이 오늘 부위를 직접 바꾼 날**은 처방을 막는다(회원 선택 덮어쓰기 방지).
    서버·화면이 **같은 판정**(`todayPlanState`)을 쓴다.
  - 화면: 회원 상세에 '운동 처방 · 오늘만' 섹션(영구 루틴 섹션 위). 코멘트 문구에
    축을 박아 "오늘만 바꿨는데 루틴이 바뀐 줄" 오해를 막는다.
  - 테스트: `member-report.test.ts`(+11) · `prescription-actions.test.ts`(+11) ·
    `trainer-prescription-schema.test.ts`(9, DDL 가드) · E2E `trainer-today-prescription.spec.ts`
  - `pnpm test` green (197 files / 2097 tests).

## 🔴 사용자 작업 대기

- **DB 마이그레이션 적용 필요** — 이 PC 에 `.env.test.local` 이 없어 라이브 DB 에 못 올렸다.
  아래 두 개를 라이브에 적용한 뒤 `pnpm test:schema` 로 확인해야 한다.
  - `supabase/migrations/202609200002_member_share_prefs.sql`
    (적용 전에는 `/settings/trainers` 가 빈 목록으로 뜬다.)
  - `supabase/migrations/202609200003_trainer_today_prescription.sql`
    (적용 전에는 '운동 처방 · 오늘만' 섹션이 안내만 뜨고 저장이 안 된다.)

## ⏭ 남은 일 / 이월

- ✅ (해소) `trainer_member_report`·`trainer_prescribe_exercise` 동의 확인 — 그 세션이 이미 넣었다.
- ✅ (해소) `loading.tsx` `RouteLoading` 누락 → 위 (7).
- ✅ (해소) '오늘만' 처방 축 → 위 (8).
- ⚪ 하단 바 홈 가운데 고정 — 이 세션 범위 밖(위 (5)). 미적용.
- 9/14 (1) 안드로이드 뒤로가기, (3) 모달 뒤로가기 — 사용자 OK 대기.
- 9/13 (1)~(4) 상태 점검·SW 캐싱·영상 캐싱·루틴 추천글 제목 — 사용자 OK 대기.
- 9/13 파일의 🟡/🔴/⚪ 항목 전부 이월(상세는 `2026-09-13.md`).

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688. 브랜치 `feat/ui-simplify`.
> ⚠️ 같은 폴더에서 **다른 에이전트(Codex)가 동시에 작업 중**(음식 검색·식단 보드).
> 커밋할 때 내 파일만 골라 담는다.

## 🔵 오늘 요청

- **(1) "원인 확인하고 고쳐줘" / "다하고 커밋해줘봐"** — 🟡 진행 (직렬 E2E 실패 37 → 7)
  - **원인 A — 접근성 이름 충돌 (내 커밋 `9c2a4d1`). 고침 `972fb1f`.**
    운동탭 머리글 `<nav aria-label="운동 도구">` 가 운동 고르기 버튼의
    `aria-label="운동"` 을 가렸다(이름 찾기는 부분 일치). → `트레이닝 도구` 로.
  - **원인 B — `.app-container` 의 `animation: … both` (1단계 `2c75cae`). Codex 가 고침 `ea25c39`.**
    `both` 가 끝난 뒤에도 `transform` 을 남기는데, **transform 이 남은 조상은
    `position: fixed` 의 기준**이 된다 → 바텀시트·모달이 화면이 아니라 본문에 갇혔다.
  - **원인 C — 모달을 닫자마자 이동하면 이동이 되돌려짐. 고침 `d291c7b`.**
    `useBackClose` 가 닫힐 때 예약하는 `history.back()` 이 아직 커밋 안 된
    `router.push` 와 경쟁했다. '운동 직접 담기'만 멀쩡했던 건 거기만 안 닫고
    바로 push 했기 때문. 같은 방식으로 5곳을 맞췄다
    (전체 바꾸기·부위 추가·런닝 대체·런닝 유지·운동 등록).
  - **원인 D — 테스트가 옛 마크업을 봄. 고침 `d291c7b`.**
    `ul.space-y-2` 순서로 워밍업·본운동·마무리를 찾던 7개 스펙을
    `data-testid=today-{warmup,main,cooldown}-list` 로 갱신.
  - **⚠️ 배운 것: `--workers=4` 병렬 E2E 는 이 프로젝트에서 못 믿는다.**
    라이브 DB·dev 서버를 공유해 간섭이 난다(설정이 `workers: 1` 인 이유).
    판정은 반드시 직렬로 한다. 병렬 22실패 → 직렬 7실패였다.

- **(2) "일단 너 작업분 현상태 커밋해줘"** — 완료(`972fb1f`).

- **(3) "홈을 앱 런처로 — 앱마다 홈·하단바가 바뀌게. 이해했으면 검수보고서로 / 유아이 디자인 보여줘"**
  — 🟡 **사용자 확인 대기**. 코드 변경 없음(이해 확인용).
  - 산출물: `docs/ui-launcher-review-2026-09-20.html` (폰 목업 3장 — 런처 홈 / 운동 앱 / 식단 앱).
  - 이해한 구조: 홈 상단 = 앱 아이콘 격자 + 앱별 요약 위젯 → 아이콘 탭 시 홈 자리가
    그 앱 홈으로 **교체**되고 **하단바도 그 앱 전용 메뉴로 교체**.
  - 현재 라우트 40여 개를 앱 8개(운동·식단·캘린더·그룹·커뮤니티·헬쑤쌤·펫·런처)로 묶는 안 포함.
  - **✅ 사용자 결정(9/20)**: "하단바는 앱마다 달라져도 좋다. 단 **모든 메뉴의 가운데는
    홈화면으로 고정**." → 보고서 **v2** 로 다시 만듦(가운데 원형 홈 버튼, 양옆 4칸만 교체).
  - **✅ 사용자 요구(9/20)**: "**운동탭 지금 있는 기능은 다 있어야 한다**."
    → 코드에서 운동 관련 기능 **34개 전수 조사 + 대조표**(보고서 06장). 없어지는 것 0건,
    그대로 27 · 자리만 이동 5 · 설정 속에 묻혀 있던 것 꺼냄 2(`/settings/progress` 성장 그래프,
    `/settings/score` 내 운동 점수).
    - 원칙: **하단바 4칸은 '입구'일 뿐**, 기능은 화면 안에 그대로 둔다.
      지금도 `/jog`·`/running`·`/plan/muscle`·`/exercises` 는 하단바에 없고 화면 안에서 들어간다.
    - 구현 4단계에서 이 34줄을 **체크리스트로 삼아 하나씩 눌러 확인**한다.
  - **✅ 사용자 결정(9/20) "추천대로 진행해줘"** → Q3 기존 6칸 **폐기** · Q4 홈 위젯 **3개 고정**
    · Q5 앱 격자 **편집 없음(1차 고정)** · Q6 **URL 유지** · Q7 운동 5번째 칸 **기록**.
  - **구현 완료(커밋 대기)**
    - `src/features/launcher/apps.ts` — 앱 8개(운동·식단·캘린더·그룹·커뮤니티·헬쑤쌤·펫·런처)의
      아이콘·색·소속 경로·양옆 4칸을 한 곳에. **가운데 홈은 레지스트리가 아니라
      `bottomTabsForPath()` 가 끼워 넣는다** — 앱이 홈을 빠뜨리는 게 구조적으로 불가능.
    - `src/components/bottom-nav.tsx` — 고정 6칸 → 경로별 4칸 + 가운데 원형 홈.
      `HIDDEN_PREFIXES`·그룹 앰버톤·pending 스피너·진동은 그대로. 홈 칸만 `overflow-visible`
      (솟은 원이 잘리면 안 됨).
    - `src/app/layout.tsx` — 헬쑤쌤 플래그 읽기를 하단바 → 홈(런처)으로 옮김.
    - `src/app/home/page.tsx` — 카드 스택 → 앱 격자 + 위젯 3개.
    - `src/features/launcher/streak.ts` — 연속일수(오늘 비어도 어제까지면 안 끊음).
  - **테스트**: 신규 단위 21(`launcher-apps` 14 + `launcher-streak` 7), `pnpm test:unit`
    **2186 green**. 기존 `ui-simplify` 2건은 옛 홈 순서를 보던 것이라 새 구조로 **갱신**.
    E2E `home-layout`·`home-tab`·`redesign-navigation` 갱신 + `launcher-home.spec.ts` 신규
    (앱 전환 시 하단바 교체 / 가운데 홈 좌표 5화면 동일 / **운동 기능 도달성**).
  - **⚠️ 배운 것 — Next dev 오버레이가 하단탭 클릭을 가로챈다.**
    `<nextjs-portal>` 이 화면 왼쪽 아래에 떠서 `intercepts pointer events` 로 클릭이 실패한다
    (프로덕션엔 없는 개발 도구). `tests/e2e/helpers/dev-overlay.ts` 의 `silenceDevOverlay()`
    로 포인터 이벤트만 끈다. **남은 E2E 7건 실패도 이게 원인일 수 있다 — 확인 필요.**
  - 설계 메모: 가운데 홈은 앱 레지스트리에 넣지 않고 **하단바가 항상 끼워 넣는 고정석**으로
    둔다(앱이 홈을 빠뜨리는 일이 구조적으로 불가능하게).
  - 걱정 2: ① 가운데 솟은 원형은 보통 ＋(빠른 기록) 자리 — 나중에 ＋ 필요하면 화면 내 FAB 로.
    ② 9/18 8단계·9단계 OK 대기 중이라 순서 확정 필요.

## ⏭ 남은 것 (조사 중)

- E2E 직렬 실패 **7건** — 재설계가 건드리지 않은 코드 경로로 보인다.
  근거: `guided-workout.tsx` 는 `610cf9c` 이후 무변경, `plan-editor.tsx` 변경은
  클래스·문구뿐(동작 없음). **재설계 이전부터 빨갰을 가능성** — 아직 확정 못 함.
  - `arm-routine-swap` 3건 — 팔 교환 후 DB day_index 가 안 바뀜(`toBe(1)` → 0).
  - `guided-complete-reenter` 2건 — 완료 후 '수고하셨습니다' 가 안 뜸.
  - `workout-substitute-today-only` 1건 — 대체 적용 후 `daily_plan` 이 비어 있음.
  - 기준 커밋 대조는 실패: 워크트리 `node_modules` 정션을 Turbopack 이 거부한다
    ("Symlink points out of the filesystem root"). 대조하려면 워크트리에 `pnpm install` 필요.

## ⏭ 이월

- 9/18 (1) 8단계 재설계 `9c2a4d1` — **사용자 OK 대기**.
- **9단계 준비됨(미적용)** — 손으로 그린 흰 표면 93곳(입력칸 30·판/버튼 63)을
  `app-field`/`app-panel` 토큰으로. 다크에서 표면색이 `#141414`(카드)·`#18181b`·`#27272a`
  세 가지로 갈리는 것을 하나로. 변환안 전수 검토 끝.
- 9/16 (2) 운동 영상 v3 115/1351 — 🟡. 재제작 9종(Codex 원본 생성 필요).
  이 폴더의 영상 파일은 미추적으로 그대로 둔다(다른 브랜치 작업분, 삭제 금지).
- 9/16 (3) 영상 커밋 `b7f6ec1` — 사용자 OK 대기. 실기기 재생 확인 안 함.
- 9/15 (3)(4)(5)(7)(8)(10) — 사용자 OK 대기(상세는 `2026-09-15.md`).
- 🔴 라이브 DB `e2e_` 계정 잔존 — **사용자 결정: "지우지 말고 두기"**(9/18). 지우지 않는다.
- 9/14·9/13 이월분 계속.
