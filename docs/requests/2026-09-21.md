# 2026-09-21 요청 추적

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688.
> 전날 `2026-09-20.md` 에서 미완료 항목 이월.

## 🔵 오늘 요청

- **(1) "16패널 이미지 만들어달라고 Codex 한테 넘길 프롬프트 정리해줘"** — 완료(사용자 확인 대기)
  - 인계문 `tools/media/motion-guides/_repanel-16/CODEX-HANDOFF.md` 는 다른 세션이 09:54 에 이미
    작성해 뒀다. 새로 쓰지 않고 **비어 있던 "나머지 plan.json 작업"** 을 실제 생성 지시로 채웠다.
  - 채운 내용: PNG 저장 경로·파일명 규칙(`~/.codex/generated_images/<배치폴더>/<운동ID>.png`),
    배치 폴더를 분리해야 하는 이유, 나눠서 여러 번 돌리는 법, reverse 19종 / full 5종 목록과
    full 을 reverse 로 바꾸면 안 되는 이유, 등록 전 원본 16칸에서 걸러야 할 5가지,
    손대지 말 것(우드초퍼·등척성 홀드 3종), 렌더 후에도 재검토 전까지 비공개라는 점.
  - **plan.json 에서 `cable-woodchopper` 를 `done_regenerated` 로 분리.** 다른 세션이 이미 16패널로
    재등록(`cutout:false`)했는데 `groupA_convert` 에 남겨 두면 `repanel-16.mjs` 가 옛 프롬프트로
    덮어쓰고 `cutout:false` 가 날아간다. 남은 대상 **24종**.
  - `CODEX-HANDOFF.md` 가 `_repanel-16/*` 규칙에 걸려 **git 에 안 올라가고 있었다** — plan.json 과
    같은 이유로 `.gitignore` 예외 추가.
  - 실측: 24종을 한꺼번에 등록하면 현재 공개 중인 6종이 빠져 **공개 82 → 76 일시 하락**
    (behind-the-back-wrist-curl, dumbbell-shoulder-press, kettlebell-row, leg-press-2, triceps-dip, trx-row).
  - 커밋 `28ec164`.

- **(2) ".md 랑 .env 중 다른 컴퓨터에 옮길만한 것 정리"** — 완료(사용자 확인 대기)
  - `docs/MACHINE-MIGRATION.md` 작성. **git clone 으로 따라오지 않는 것만** 정리했고
    원칙대로 **비밀값은 적지 않고 키 이름만** 적었다(작성 후 실제 값과 대조해 누출 0 확인).
  - 🔴 **가장 중요한 발견: `health_app` 에 미푸시 커밋 6개**(브랜치 `feat/full-ui-renewal`).
    새 컴퓨터에서 그냥 clone 하면 사라진다. 푸시하거나 `.git` 폴더째 복사해야 한다.
    `heltch-admin` 은 미푸시 0.
  - **.md 는 따로 옮길 것이 없다** — 79개 전부 git 추적 중이고 미추적 .md 는 없다.
    (무시 목록에 뜨는 .md 는 node_modules·.venv-cutout 라이선스·test-results 리포트뿐.)
  - **.env 4개는 반드시 손으로 옮긴다**(`.gitignore` 의 `.env*`):
    `health_app/.env`(2키) · `.env.local`(21키) · `.env.test.local`(9키) · `heltch-admin/.env.local`(2키).
    `.env.test.local` 이 없으면 E2E 와 스키마 가드가 통째로 skip 된다.
  - 그 밖에 git 밖 자산: `tools/media/imports/`(24MB, 재생성 불가 — 옮긴다),
    `~/.codex/generated_images/`(667MB — 현재 참조분만 선별), `android/local.properties`(컴퓨터마다 달라 새로 만듦),
    `.claude/settings.local.json`(선택). `node_modules`·`.venv-cutout`·`.next` 는 재생성.
  - 안드로이드 서명 키스토어는 이 저장소에 **없다** — 따로 보관 중이면 그것도 챙겨야 한다.

- **(3) "D:/git 하위 다른 폴더의 .env·정보 파일도 정리"** — 완료(사용자 확인 대기)
  - `D:\git` 과 `C:\git` 전체를 훑어 결과를 **`D:\git\SECRETS-CLEANUP-20260921.md`** 에 정리했다.
    이 파일은 어떤 저장소에도 속하지 않아 커밋되지 않는다. 값은 한 글자도 적지 않았다.
  - 🔴 **`C:\git\wlpp-wl-manager` 에 `.env.local`·`.env.dev`·`.env.prod` 가 커밋·푸시돼 있다.**
    셋 다 `MONGODB_URI` 가 `사용자:비밀번호@호스트` 형태의 실제 접속문자열이고 히스토리에도 남아 있다.
    원격은 AWS CodeCommit. → 비밀번호 교체가 먼저다.
  - 🔴 **`spring_key.p12` 동일 파일이 4개 저장소에 커밋**돼 있다(mt-coocon-was ×2, grefin-aml-was,
    bizplay-aml-was). 개발용 자체서명인지 운영 인증서인지 확인 필요.
  - 🟡 `health_app` 원격 하나를 **세 폴더가 체크아웃**(`health_app_b2b`·`health_app_ui`·`heltch/health_app`)하고
    `.env.test.local` 은 **3곳 모두 동일 사본**, `.env`·`.env.local` 은 2곳 동일. 키 교체 시 누락 위험.
  - ✅ 확인된 안전: `.env` 가 커밋된 저장소는 app-wl-manager 하나뿐이고 나머지 8개는 히스토리에도 없다.
    문서·설정 파일에서 접속문자열·개인키·AWS/GitHub/Slack 토큰 패턴은 **0건**.
  - 덤으로 `docs/MACHINE-MIGRATION.md` 의 "미푸시 6개" 를 고쳤다. 다른 세션이 `480676c` 를 커밋·푸시해
    1개로 줄어 있었다 — 숫자를 박아두지 말고 옮기기 직전에 다시 세라고 바꿨다.

## 📌 검증 기록 (2026-09-21 재확인)

커밋 `ee0d7b2`·`28ec164`·`6919a7c` 의 내용을 다시 돌려 확인했다.

- `pnpm test` green — **198파일 / 2159테스트**(로직 단위 + 스키마 동기화 가드).
- `tsc --noEmit` 통과, 추가한 E2E 파일 eslint 오류 0.
- **E2E `workout-motion-dark-video.spec.ts` 재실행 통과**(30.9s). 운동모드에서
  `high-bar-squat.mp4` ↔ `high-bar-squat-dark.mp4` 교체와 480정사각 8초 실디코드를 다시 확인했다.
- `repanel-16.mjs` 를 빈 디렉터리로 돌려 **대상 24종**(cable-woodchopper 빠짐)을 읽는 것과
  PNG 가 없으면 아무것도 등록·렌더하지 않는 것을 확인했다.
  실행 후 `reviews.json`·`verification.json` 의 변경 종목은 여전히 `cable-woodchopper` 하나뿐이고
  (= 다른 세션 작업), `manifest-dark.json` 은 커밋본과 **내용 동일**(줄바꿈만 다름).
- 🔴 실기기(Android) 재생은 여전히 미확인.

### 포트 충돌 재발 — 확인 방법

`next dev` 는 같은 디렉터리에서 두 번째 서버를 거부한다. 어제 띄운 서버(3001, PID 4232)가
살아 있어 새로 띄운 3210 이 즉시 죽었다. TaskStop 은 래퍼 셸만 죽이고 node 는 남는다.
→ E2E 전에 `Get-NetTCPConnection -State Listen -LocalPort 3000,3001,...` 로 **실제 리스닝 포트**를
확인하고 `E2E_BASE_URL` 을 거기에 맞춘다. 기본값 3000 을 믿으면 남의 서버를 때린다.

## ⏭ 이월 (2026-09-20 에서)

- **16패널 A군 24종 이미지 생성** — 🔴 이 세션 불가(imagegen 은 Codex 도구). 인계문 준비 완료.
  PNG 가 나오면 `node tools/media/repanel-16.mjs <폴더>` 로 등록·렌더까지 이어지고, 재검토는 사람이 한다.
- **`cable-woodchopper` 후보6 재검토** — 다른 세션 진행 중(중간 손·머리 잔상, 8→9 자세 간격).
  아직 pending. 미커밋 상태라 건드리지 않았다.
- **실사 사진 스타일 원본 8종** — 사용자 결정 대기. 이 중 6종은 16패널 재생성으로 자동 해결.
- **`tools/media/imports/` (21MB) 커밋 여부** — 사용자 결정 대기(rembg 모델 바이너리 4.4MB 포함).
- **`bash.exe.stackdump`** — 무관한 크래시 덤프. 제외 중.
- 🔴 **실기기(Android) 재생 확인** — APK 필요, 사용자 몫.
- 9/14·9/13 의 🟡/🟦/🔴/⚪ 항목 전부 이월 (상세는 각 날짜 파일).

## ✅ 완료 (9/20 에서 확정)

- 누끼 재렌더 104종 전량 완료(`done; failed 0`) + 재검토 대기 0. 커밋 `39bc207`.
- 앱 화면 E2E 실행: `demo-video-fits-phone` 8/8 green(86 페이지 실재생).
  운동모드 라이트/다크 전환 회귀 `workout-motion-dark-video.spec.ts` 추가. 커밋 `ee0d7b2`.

> 브랜치: `feat/trainer-member-management`

## ⚠ 이 작업 트리는 **다른 세션과 동시에 작업 중**이다

사용자 결정(2026-09-20 23:0x): **이 세션은 B2B / 트레이너 화면 쪽만** 맡는다.

- 다른 세션 소유(건드리지 않음): `src/components/bottom-nav.tsx`(하단 바 재작성 중) ·
  `src/features/launcher/**` · `src/app/routine/records/**`
- 이 세션 소유: `/groups/[id]/trainer/**` · `features/groups/**`(트레이너·처방·공유설정) ·
  트레이너 관련 `supabase/migrations/2026092000*`

## 🔴 지금 막혀 있는 것 (사용자 조치 필요)

- **라이브 DB 마이그레이션 적용** — 사용자가 "니가 적용해줘" 로 승인했으나,
  **자동모드 분류기가 프로덕션 DB 쓰기를 차단**해서 Claude 가 실행하지 못했다.
  적용 대상(순서대로, 전부 추가형 DDL — 기존 데이터/테이블 안 건드림):
  1. `supabase/migrations/202609200002_member_share_prefs.sql` (테이블 + RLS + 함수)
  2. `supabase/migrations/202609200001_trainer_member_management.sql` (함수)
  3. `supabase/migrations/202609200003_trainer_today_prescription.sql` (함수)
  - 적용 스크립트(파일별 트랜잭션, 실패 시 롤백):
    `C:\Users\admin\AppData\Local\Temp\claude\D--git-health-app-b2b\e185e350-14a9-4b9a-961d-c1240cf18b35\scratchpad\apply-migrations.mjs`
  - 적용 후 `pnpm test:schema` 로 확인.
  - 미적용 상태의 증상: `member_share_prefs` 테이블 없음 → `/settings/trainers` 빈 목록,
    '운동 처방 · 오늘만' 섹션이 안내만 뜨고 저장 실패.
- `.env.test.local` 은 `D:/git/heltch/health_app` 에서 복사해 왔다(gitignore 대상).
  같은 저장소(`bit-O-study/health_app`)의 다른 작업 사본 = 같은 DB.

## ⏭ 이월 (2026-09-20 에서)

- 🟡 **9/20 (3)(4)(6)(7)(8) 사용자 OK 대기** — 상세는 `2026-09-20.md`.
  - (3) 홈 헤더 트레이너 모드 전환 / (4) 회원의 정보 제공 설정·트레이너 제거
  - (6) 트레이너 화면 '내 운동' 복귀 스위치 / (7) `pnpm test` 복구
  - (8) '오늘만' 처방 축 (커밋 `1a121a7`)
- ⚪ 하단 바 홈 가운데 고정 — **이 세션 범위 밖**(다른 세션이 재작성 중). 미적용.
- 9/14 (1) 안드로이드 뒤로가기, (3) 모달 뒤로가기 — 사용자 OK 대기.
- 9/13 (1)~(4) 상태 점검·SW 캐싱·영상 캐싱·루틴 추천글 제목 — 사용자 OK 대기.
- 9/13 파일의 🟡/🔴/⚪ 항목 전부 이월(상세는 `2026-09-13.md`).

## 🟠 다른 세션에 알릴 것 (이 세션이 안 고침)

- `tests/be/logic/safe-area.test.ts` **실패 중** — 재작성 중인 `bottom-nav.tsx` 의
  하단탭 높이와 `features/community/components/post-detail.tsx` 의
  `bottom-[calc(...)]` 가 어긋났다.
- 같은 파일에 `aria-label="?? ??"` 처럼 **한글이 깨진 문자열**이 있다.

## 🔵 오늘 요청

(아직 없음)

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688. 브랜치 `feat/ui-simplify`.
> ⚠️ 같은 폴더에서 **다른 에이전트(Codex)가 동시에 작업 중**(음식 검색·식단 보드).
> 커밋할 때 내 파일만 골라 담는다. **E2E 도 동시에 돌아 라이브 DB 가 섞인다**(아래 배운 것 참고).

## 🔵 오늘 요청

- **(1) 런처형 홈 — 9/20 요청 이어서 구현** — 🟡 **사용자 OK 대기**(코드는 끝, 커밋 대기)
  - 확정된 설계(9/20): 홈 = 앱 아이콘 판 + 요약 위젯 3개 / 앱에 들어가면 화면·하단바가
    그 앱 것으로 교체 / **하단바 가운데(3번째) 칸은 어느 앱에서도 홈 고정**.
  - "추천대로 진행" → 기존 6칸 **폐기** · 위젯 **3개 고정** · 앱 격자 **편집 없음** ·
    **URL 유지** · 운동 앱 5번째 칸 **기록**.
  - 검수보고서: `docs/ui-launcher-review-2026-09-20.html` (v2, 라인 아이콘).

### 만든 것

| 파일 | 내용 |
| --- | --- |
| `src/features/launcher/apps.ts` | 앱 8개의 아이콘·색·소속 경로·양옆 4칸. **가운데 홈은 레지스트리가 아니라 `bottomTabsForPath()` 가 끼워 넣는다** — 앱이 홈을 빠뜨리는 게 구조적으로 불가능 |
| `src/features/launcher/streak.ts` | 연속 운동일수(오늘이 비어도 어제까지 이어졌으면 안 끊음) |
| `src/features/launcher/components/app-grid.tsx` | 홈 상단 앱 아이콘 격자 |
| `src/features/launcher/components/app-widget.tsx` | 앱별 요약 한 줄 위젯 |
| `src/components/bottom-nav.tsx` | 고정 6칸 → 경로별 4칸 + 가운데 원형 홈 |
| `src/app/layout.tsx` | 헬쑤쌤 플래그 읽기를 하단바 → 홈으로 |
| `src/app/home/page.tsx` | 카드 스택 → 앱 격자 + 위젯 3개 |
| `src/app/settings/progress/page.tsx` | 홈에서 내려온 **이번 주 요약 카드**를 여기(운동 앱 '기록' 칸)에 한 장 |

### 테스트

- 신규 단위 21건 — `launcher-apps`(14) · `launcher-streak`(7). `pnpm test:unit` **2186 green**.
- 갱신: `ui-simplify` 2건(옛 홈 순서·이번 주 카드 위치), E2E `home-layout`·`home-tab`·
  `redesign-navigation`·`weekly-training`.
- 신규 E2E `launcher-home.spec.ts` — 앱 전환 시 하단바 교체 / **가운데 홈 좌표가 5개 화면에서 동일** /
  **운동 기능 도달성**(루틴·운동찾기·성장그래프·근육별선택·오늘만바꾸기·운동점수).
- 직렬 통과: launcher-home 3 · home-tab 1 · home-layout 3 · redesign-navigation 2 ·
  weekly-training 11 · community-no-group-tab 2 · notification-settings 4 · back-link 1 ·
  account-navigation 1.

### ⚠️ 오늘 배운 것 (다음에 또 걸림)

1. **Next dev 오버레이가 하단탭 클릭을 가로챈다.**
   `<nextjs-portal>` 이 화면 왼쪽 아래에 떠서 `intercepts pointer events` 로 30초 재시도 후 실패.
   프로덕션엔 없는 개발 도구 → `tests/e2e/helpers/dev-overlay.ts` 의 `silenceDevOverlay()` 로
   포인터 이벤트만 끈다. **9/20 남은 E2E 7건 실패도 이게 원인일 수 있다 — 확인해 볼 것.**
2. **E2E 그룹 이름이 실행별로 격리돼 있지 않았다.**
   이메일만 `E2E_RUN_ID` 로 격리돼 있고 그룹 이름은 고정 문자열이라,
   Codex 와 동시에 E2E 를 돌리면 `where name='E2E 주간분석'` 이 2건을 물어 왔다.
   → `run-scope.ts` 에 `scopedName()` 추가.
3. **미래 날짜로 완료 기록을 심으면 화면마다 숫자가 갈린다.**
   `seedCompletion({ dayOfWeek: 2 })` 는 이번 주 수요일이라 **월·화엔 미래**가 된다.
   트레이너 쪽(`getGroupMemberWeeklyTraining`)엔 `.lte("for_date", today)` 가 있고
   회원 본인 화면엔 없어서, 같은 주 월·화에만 "트레이너가 같은 숫자를 본다" 가 깨졌다.
   → 월요일(0)로 심어 날짜에 안 흔들리게. (제품 쪽 불일치 자체는 그대로 둠 — 미래 완료 기록은
   원래 생길 수 없는 상태라 급하지 않다. 정리하려면 별도 요청으로.)

- **(2) "E2E 7건도 그 오버레이 문제인지 확인해줘"** — ✅ 조사 완료(코드 변경 없음).
  - **결론: 오버레이 문제가 아니다.** 실패 리포트 어디에도 `nextjs-portal` 이 없다.
    전부 DB 값이 안 맞는 단언 실패다.
  - 현재 실패는 **7건이 아니라 4건**이다. `guided-complete-reenter` 2건은 지금 통과한다.
    남은 건 `arm-routine-swap` 3 + `workout-substitute-today-only` 1.
  - **진짜 원인(실험으로 확정): 확인 모달이 서버액션보다 먼저 닫혀서 액션이 취소된다.**
    - 증거: `POST http://localhost:3000/plan` 가 `net::ERR_ABORTED` 로 끊긴다.
      끊긴 뒤 `routine_exercises.day_index` 도 `custom_week` 도 **하나도 안 바뀐다**
      (before/after 스냅샷이 완전히 동일).
    - 경로: `ConfirmDialog.onConfirm` 이 `doSwapArmRoutine(...)` 을 부르고 **곧바로
      `setConfirm(null)`** → `useBackClose` 가 `history.back()` 을 예약 → 그 이동이
      아직 날아가는 중인 교환 POST 를 끊는다. (`plan-editor.tsx` 의 ConfirmDialog 부분)
    - **9/20 '원인 C'와 같은 계열**이다. 그때 5곳(전체 바꾸기·부위 추가·런닝 대체·런닝 유지·
      운동 등록)을 고쳤는데 **팔 교환만 빠졌다.**
    - 확인 실험: `arm-swap` 일 때 `setConfirm(null)` 을 안 하도록 바꾸니 **그 테스트가 통과**했다.
      단 그 방식은 "확인 후 모달이 닫힌다"를 기대하는 다른 테스트를 깨뜨린다
      (`교환 확인 모달은 배경을 inert…`, `미저장 팔 편집은 …차단된다`). **실험 패치는 원복함.**
  - 제대로 고치려면: 모달은 닫되 **액션이 날아가는 동안엔 뒤로가기 이동을 막는**
    방식이 필요하다(예: in-flight 중엔 `useBackClose` 의 history 복원을 미루기).
    이건 `modal-history.ts` 를 건드리는 일이라 **별도 요청으로 받는 게 안전하다.**
- **(3) "그것도 고쳐줘 / 검색 누르면 메인 페이지가 나온다"** — ✅ 완료(커밋).
  - **모달·액션 경쟁 수정(`c427d5c`)** — `onConfirm` 을 async 로 바꿔 **작업이 끝난 뒤에** 닫는다.
    막힌 경우(미저장 편집)는 즉시 끝나 예전처럼 바로 닫힌다.
    ⚠ 닫는 시점만 미루니 **반대 경쟁**이 생겼다 — 새로고침·이동이 걸린 뒤에 닫으면
    그 `back()` 이 화면 전환을 취소해 버튼이 계속 비활성으로 남는다.
    그래서 **화면을 통째로 바꾸는 성공 경로에선 아예 닫지 않는다**(전환에 맡김).
    같은 모양을 쓰던 **다섯 경로 전부** 맞췄다(팔 교환·전체 추천·부위 추천·일차 추천·전체 비우기).
  - **런처 '검색' 칸 수정(`1475bea`)** — 원인은 "동작 안 함"이 아니라 **남의 앱으로 넘어감**이었다.
    `/exercises` 는 운동 앱 땅이라 도착하는 순간 하단바가 운동 앱 것으로 갈렸다
    (dev 에선 그 라우트를 그 자리에서 컴파일하느라 몇 초간 홈이 남아 더 그렇게 보였다).
    → 검색을 빼고 **체형**(`/settings/body-composition`)을 넣었다. 운동 종목 찾기는
    원래대로 운동 앱의 '운동찾기' 칸이 담당한다.
    - 가드를 넣고 보니 **같은 실수가 하나 더** 있었다 — 펫 앱 '기록' 칸이 `/calendar` 를
      가리켜 캘린더 앱으로 넘어갔다. `/settings/history` 로 교체.
    - 재발 방지: 단위 가드 2개(런처 칸·앱 칸) + E2E 1개(런처 칸을 눌러도 하단바 그대로).
  - **남은 E2E 전부 해결 — 9/20 기준 실패 0.** 직렬 20/20.
    - `arm-routine-swap` 15/15 (모달 경쟁 수정으로 3건 해결).
    - `workout-substitute-today-only` — **제품 버그 아니었다.** 화면이 먼저 바뀌고 DB 가
      조금 늦게 따라오는데 테스트가 **한 번만 읽어** 빈 배열을 보고 실패했다.
      `expect.poll` 로 조건이 될 때까지 다시 읽게 고쳤다.
    - `팔 교환 요청 중에는 …` — **테스트가 엉뚱한 서버액션을 붙잡고 있었다.**
      `page.route("**/*")` 를 처음부터 걸어 둬서, 교환이 아니라 먼저 날아간 다른
      서버액션(앱 이벤트 보고 등)을 붙잡고 화면이 멈춰 확인 모달조차 안 떴다.
      → 붙잡기를 **교환 누르기 직전에만** 걸도록 옮겼다.
  - **⚠️ 배운 것 — "E2E 가 빨갛다 = 제품이 깨졌다" 가 아니다.**
    이번 4건 중 제품 버그는 1건(모달 경쟁)뿐이고, 나머지는 테스트가
    ① DB 를 너무 일찍 읽거나 ② 남의 요청을 붙잡거나 ③ 미래 날짜로 심어서였다.
    빨간 걸 볼 때 **실패 리포트의 실제 메시지부터** 읽는다(추측하지 않는다).

- **(4) "기능·버튼 중복된 거 다 없애줘"** — ✅ 완료(`b2d4666`).
  - **가장 큰 것: 눌러도 같은 화면으로 가는 칸 10개.** 어제 칸을 4개로 맞추려고
    `/diet?tab=search` 같은 걸 넣었는데 그 페이지들은 `tab` 을 읽지도 않는다.
    식단·커뮤니티는 **버튼 넷이 전부 같은 화면**이었고, 그 기능들은 이미 화면 안에
    버튼·탭으로 있다(커뮤니티 상단 오운완/운동/내 글, 식단 검색/직접입력/AI사진).
  - **새 규칙: 앱은 실제로 가진 화면만 칸으로 갖는다.** 칸은 **0·2·4개만** 허용하고
    홈은 언제나 정확히 한가운데(3칸 또는 5칸). **0개면 런처 바를 그대로 쓴다.**

    | 앱 | 하단바 |
    | --- | --- |
    | 운동 | 오늘 · 루틴 · [홈] · 운동찾기 · 기록 |
    | 캘린더 | 달력 · [홈] · 주기 |
    | 그룹 | 내 그룹 · [홈] · 관리 |
    | 펫 | 펫 · [홈] · 다짐 |
    | 런처 | 체형 · 기록 · [홈] · 알림설정 · 나 |
    | 식단·커뮤니티·헬쑤쌤 | 화면이 하나뿐 → 런처 바 그대로 |

  - **같은 화면을 두 칸이 가리키던 것**도 정리: `/settings/score`(캘린더 '통계' ↔ 펫 '보상'),
    `/settings/history`(캘린더 '지난 기록' ↔ 펫 '기록'), `/settings/progress`(운동 '기록' ↔ 런처 '기록').
  - **한 화면 안의 중복 3건**
    - `/routine` 머리글 '운동 찾기'(자연어) ↔ 하단 '운동찾기'(카탈로그) — 이름이 거의 같아
      구분이 안 됐다. 자연어 찾기를 `/exercises` 안으로 옮겨 **찾는 곳을 하나로**.
    - 홈 머리글 설정 아이콘 ↔ 하단 '나' — 둘 다 `/settings`. 아이콘 제거.
    - 런처 '알림' 칸(= 알림 **설정**) ↔ 알림 **벨**(= 알림 **목록**) — '알림설정'으로 개명.
  - 가드: **같은 화면을 두 칸이 가리키면 단위테스트가 잡는다.**
  - **✅ 사용자 결정(9/21)**: 홈의 **앱 아이콘 ↔ 요약 위젯**이 같은 앱으로 가는 것은
    **지금처럼 둔다.** (아이콘은 이동, 위젯은 정보 + 지름길 — 역할이 다르다.
    아이폰 홈 화면과 같은 구조. 중복으로 보지 않기로 함.)

### 남은 것

- 🟡 **사용자 확인** — 실제 화면 보고 "이대로 OK" 받기(받기 전엔 완료 아님).
- 앱별 홈 채우기(4단계)는 운동·식단 위주로만 했다. 캘린더·그룹·커뮤니티·펫은
  기존 화면을 앱 홈으로 지정만 한 상태 — 각 앱 홈을 다듬는 건 다음 단계.
- Codex 분담 제안(사용자 질문 "같이 나눠서 하면 빠르지?")에 대한 답:
  1~3단계는 직렬이라 못 쪼갠다. **레지스트리 커밋 후** 식단/그룹/커뮤니티 앱 홈을 Codex 가
  맡는 건 가능. 단 `apps.ts`·`bottom-nav.tsx`·`layout.tsx` 는 나만 건드린다.

## ⏭ 이월 (9/20 에서)

- ~~E2E 직렬 실패 7건~~ — ✅ **2026-09-21 전부 해결.** 위 (3) 참고.
- 9/18 (1) 8단계 재설계 `9c2a4d1` — 사용자 OK 대기.
- **9단계 준비됨(미적용)** — 흰 표면 93곳을 `app-field`/`app-panel` 토큰으로.
  ⚠️ 런처 전환으로 홈이 통째로 바뀌었으니 **변환 대상을 다시 세어야 한다.**
- 9/16 (2) 운동 영상 v3 115/1351 — 재제작 9종(Codex 원본 생성 필요).
  이 폴더의 영상 파일은 미추적으로 그대로 둔다(다른 브랜치 작업분, 삭제 금지).
- 9/16 (3) 영상 커밋 `b7f6ec1` — 사용자 OK 대기. 실기기 재생 확인 안 함.
- 9/15 (3)(4)(5)(7)(8)(10) — 사용자 OK 대기(상세는 `2026-09-15.md`).
- 🔴 라이브 DB `e2e_` 계정 잔존 — **사용자 결정: "지우지 말고 두기"**(9/18). 지우지 않는다.
- 9/14·9/13 이월분 계속.
