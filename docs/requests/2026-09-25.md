# 2026-09-25 요청 추적

> 커밋은 로컬만(푸시·PR·배포는 사용자). 작성자 bong94688. 브랜치 `integration/pending-branches`.
> 전날까지(`2026-09-21.md`) 미완료 항목 이월.
> ⚠️ 이 작업 트리는 **다른 세션과 동시에 작업 중**(트레이너·런처·UI 정리). 커밋할 때 내 파일만 담는다.

## 🔵 오늘 요청

- **(1) "로그인 안 되는 것 같은데 확인해봐" (bong9468@naver.com)** — ✅ **확인·수정 완료**(사용자 OK 대기)

  ### 계정은 멀쩡했다

  - Supabase Auth 에 비밀번호 로그인 직접 호출 → **HTTP 200, 세션 발급 정상.**
  - 프로필도 정상: 정지·영구정지·탈퇴·임시비번(`must_change_password`) 전부 해당 없음
    (미들웨어가 `/suspended`·`/change-password` 로 튕기는 경우가 아니었다).
  - JWKS(`kid 570107f5…`)도 토큰과 일치 — `getClaims()` 로컬 검증 실패 경로도 아니었다.

  ### 진짜 원인 — **하이드레이션 전에 입력하면 로그인 버튼이 먹통**

  프로덕션(`health-app-five-iota.vercel.app`)에서 재현했다.

  - 로그인 폼 입력은 전부 **제어 컴포넌트**(`value={state}`)다. 첫 화면은 서버 HTML 이고
    React 하이드레이션은 조금 뒤에 끝나는데, 그 사이에 타이핑하거나 브라우저·비밀번호
    매니저가 자동완성하면 값이 **DOM 에만** 들어간다(onChange 가 아직 안 붙었다).
  - 그래서 **화면엔 이메일·비밀번호가 멀쩡히 보이는데** 로그인을 누르면
    "이메일과 비밀번호를 입력해 주세요" 가 뜬다. 필드를 다시 건드리기 전엔 계속 안 된다.
  - 앱 WebView·느린 네트워크처럼 하이드레이션이 늦을수록 잘 걸린다.
    재현 로그: JS 청크를 6초 지연시키고 입력 → `[하이드레이션 후] email="bong9468@naver.com"`
    인데 제출은 "이메일과 비밀번호를 입력해 주세요".

  ### 고친 것 (커밋 `480676c` · `d3cc07c`)

  - `src/lib/forms/prefilled.ts` — 순수 로직. DOM 에만 있는 값을 골라 state 와 합친다
    (빈 값으로 state 를 지우지 않는다).
  - `src/lib/forms/use-prefilled-inputs.ts` — 마운트(=하이드레이션) 직후 1회 끌어올리는 훅.
  - 제출 시엔 state 대신 **폼 DOM 의 실제 값**(`withPrefilled`)으로 검증·전송한다.
  - `src/lib/use-hydrated.ts` + `<fieldset disabled={!hydrated}>` — 핸들러가 붙기 전엔
    아예 조작을 막는다(두 겹 방어).
  - **인증 폼 4개 전부** 적용: 로그인/가입(`auth-form`) · 아이디 찾기 · 비밀번호 찾기 ·
    비밀번호 변경. (한 곳만 고치면 다른 화면으로 들어온 사용자에게 같은 버그가 남는다.)

  ### 테스트

  - 단위 `tests/be/logic/prefilled-inputs.test.ts` **15건 통과** — 값 고르기 규칙 +
    **인증 폼 4개가 전부 이 보호를 달고 있는지 가드**.
  - E2E **4건 통과**(프로덕션 빌드 `next start` 127.0.0.1:3000 기준, 2026-09-25 재확인):
    - `auth-hydration.spec.ts` 2건 — 스크립트 준비 전 입력 차단 / 준비 후 첫 조작 처리.
    - `real-account-login.spec.ts` 2건 — **실계정 로그인 → 오늘의 운동 정상**,
      **입력 이벤트 없는 자동완성 값으로도 로그인**(비밀번호 매니저 케이스).

  ### 남은 확인

  - 🟡 사용자 실기기에서 "이제 로그인 된다" 확인 — 웹 배포 후. (네이티브 코드 변경 아님 → APK 재빌드 불필요.)

## 🟠 다른 세션에 알릴 것 (이 세션이 안 고침)

`pnpm test` 전체 결과: **216파일 / 2468테스트 중 11건 실패** — 전부 내 작업 밖(트레이너·런처·UI 정리)이다.

- `tests/be/schema-sync.test.ts` — 라이브 DB 에 **`member_share_prefs` 테이블 없음**.
  9/21 문서의 "라이브 DB 마이그레이션 미적용"이 그대로다(사용자 조치 대기).
- `tests/be/logic/route-loading-coverage.test.ts` — 새 화면 4개(`/admin/trainers` 등)에 `loading.tsx` 없음.
- `tests/be/logic/ui-simplify.test.ts` 9건 — 새로 들어온 트레이너·그룹 화면이 UI 규칙 위반
  (emerald-* 41곳, 11px 19곳, 임의 글자 크기 6곳, `PageHeader` 없는 하위 화면 등).

## ⏭ 이월 (2026-09-21 에서)

- **16패널 A군 24종 이미지 생성** — 🔴 imagegen 은 Codex 도구. 인계문 준비 완료.
- **`cable-woodchopper` 후보6 재검토** — 다른 세션 진행 중.
- **실사 사진 스타일 원본 8종** / **`tools/media/imports/` 커밋 여부** — 사용자 결정 대기.
- 🔴 **실기기(Android) 영상 재생 확인** — APK 필요, 사용자 몫.
- 🔴 **라이브 DB 마이그레이션 3개 적용** — 사용자 조치 대기(위 schema-sync 실패 원인).
- 9/21 (1)(2)(3) 16패널 인계문·머신 이전 문서·비밀정보 정리 — 사용자 확인 대기.
  - 🔴 `C:\git\wlpp-wl-manager` 의 커밋된 `.env*`(MongoDB 접속문자열) 비밀번호 교체가 먼저다.
- 런처형 홈 / 중복 버튼 정리 / E2E 수정 — 사용자 OK 대기(상세는 `2026-09-21.md`).
- 9/18·9/16·9/15·9/14·9/13 의 🟡/🟦/🔴/⚪ 항목 전부 이월(상세는 각 날짜 파일).

## 📌 배운 것 — 이 컴퓨터의 실행 환경

- **PATH 의 Node 는 v20.16 이라 이 저장소 도구가 안 돈다**(pnpm 11 은 ≥22.13, vitest 4/vite 8 은
  require(ESM) 필요). 실제로 쓰이는 건
  `C:\Users\NB0075\AppData\Local\npm-cache\_npx\b5cf8cd106d37931\node_modules\node\bin\node.exe` (v24.21).
  `corepack pnpm` 은 서명키 오류(`Cannot find matching keyid`)로 실패한다.
- 포트 3000 은 `next start`(프로덕션 빌드)가 **127.0.0.1 에만** 물려 있다.
  `localhost` 로 붙으면 IPv6 로 다른 프로젝트(MeetingMinutes)에 닿는다 — E2E 는 `127.0.0.1` 로.
- `next dev` 는 같은 디렉터리에서 두 번째 서버를 거부한다(먼저 뜬 PID 를 확인할 것).

- **(2) "일단 런해줘봐" → "그것도 고쳐줘"(하단 탭 뒤로 글자 비침)** — ✅ 완료(사용자 OK 대기)
  - 앱 실행: Node 24(`_npx` 캐시)로 `next dev -p 3100`. 로그인 → `/home` → `/routine` 까지
    실제로 몰아 봄(JS 에러 0, HTTP 4xx/5xx 0).
  - 발견: 홈 아래쪽 "이번 주 요약" 숫자가 **하단 탭 바 뒤로 또렷이 비쳤다.**
  - 원인은 여백이 아니었다(`body` 하단 패딩·스크롤 끝 겹침 모두 정상, 실측 확인).
    **빌드(Lightning CSS)가 표준 `backdrop-filter` 를 지우고 `-webkit-` 만 남겨서**
    블러가 아예 안 걸린 것 — 78% 반투명만 남아 뒤 글자가 그대로 보였다.
    컴파일된 CSS 확인: `.app-glass` 에 `-webkit-backdrop-filter` 한 줄만 있었고
    `getComputedStyle(nav).backdropFilter === "none"`.
  - 고침: `src/styles/globals.css` 의 `.app-header`·`.app-glass` 에서 **`-webkit-` 을 먼저,
    표준 속성을 뒤로** 옮겼다. 재빌드 후 두 줄 다 살아남고
    `backdropFilter === "blur(24px) saturate(1.8)"` 로 바뀐 것과 화면(뒤 글자가 흐려짐)을 확인.
  - 테스트: `tests/be/logic/glass-backdrop.test.ts` — 프로젝트 CSS 전체에서 선언 순서와
    webkit/표준 짝을 검사. **일부러 순서를 뒤집은 파일로 가드가 실제 실패하는 것까지 확인.**
  - ⚠ `bottom-nav.tsx` 자체는 다른 세션 소유라 건드리지 않았다(CSS 만 고침).

- **(3) "지금 브랜치 기반으로 feat 브랜치 만들어서 작업"** — ✅ `feat/test-green` (from `integration/pending-branches`)
  - 사용자 선택: **실패 테스트 11건 정리**.
  - `0740583` 트레이너 화면 5곳 `loading.tsx`(route-loading 가드) —
    `/admin/trainers` · `/settings/trainer-pass` · `/trainer` · `/trainer/invite/[token]` · `/trainer/members/[id]`.
  - `b0a30e1` UI 규칙 정리: emerald-* 45곳 → 브랜드 토큰, 임의 글자 크기 25곳 → `text-xs`,
    `font-black/extrabold` 4곳 → `font-bold`, 그룹원 오늘 기록 화면을 `PageHeader` + `.app-page` 로.
    다크 변형(`dark:*-emerald-*`)은 토큰이 알아서 바뀌므로 제거, 민트 바탕 흰 글씨엔 `dark:text-zinc-950`.
    캘린더 가드는 **테스트를 갱신**(주간 보기가 생겨 라벨이 조건부가 됨 — 가드 취지는 '달 이동이 머리글 안').
  - ⚠ `b0a30e1` 에 **다른 세션의 미커밋 작업이 같이 담겼다**(홈 재작성·앱 격자 편집·트레이너 처방 신규).
    파일 내용은 그대로라 그쪽 작업은 안전. 사용자 결정: **그대로 두고 나중에 완료되면 땡겨 받는다.**

- **(4) "세트 방식(드롭·피라미드·탑세트·백오프) 추가"** — ✅ 완료(사용자 OK 대기)
  - **DB 변경 없음.** `set_details`(세트별 무게·횟수)가 routine_exercises·daily_plan·
    exercise_completions 에 이미 있고 운동모드·기록·볼륨이 그걸 읽는다. 세트 방식은
    **그 배열을 채워 주는 방법**으로 만들었다(스키마 변경 0, 옛 데이터 그대로).
  - `ff2194b` 순수 로직 `set-scheme.ts` — 8종 펼치기(드롭·피라미드·역피라미드·탑세트+백오프·
    클러스터·레스트포즈·AMRAP·일반) + 묶음(슈퍼세트/트라이/자이언트) 구분.
  - **증량 단위 현실화** — 옛 규칙(바벨·머신 5kg / 나머지 1kg)은 바벨 컬에 5kg, 덤벨에 1kg 을
    권했다. 이제 종목 크기(`loadClassOf`)까지 봐서: 원판 기구 큰 종목 5kg·작은 종목 2.5kg,
    핀 스택 큰 기구 5kg·작은 기구 2.5kg, 덤벨 2kg, 케틀벨 4kg, 밴드·TRX 없음.
    헬스장마다 스택이 달라 **종목별 사용자 지정(`overrideKg`)이 기본값을 이긴다**(설정 UI 는 아직).
  - `d210b00` 계획 편집 UI — `세트 방식` 버튼에서 고르면 세트별 값이 채워지고,
    무게는 그 기구 증량 단위로 반올림한다. 저장된 숫자에서 방식 이름을 되짚어 표시.
  - `b7517cd` 무게·횟수 고정이 꺼져 있어도 입구를 보여 주고, **"켜고 계속"** 으로 켠 뒤 이어서 편집.
    개인설정 전체에 걸리는 값이라 말없이 켜지 않는다. `setPersonalPrefAction` 이 `/plan`·
    `/plan/today` 캐시를 안 지우고 있어 같이 무효화(켠 직후 옛 화면이 남던 문제).
    계획 편집·오늘만 편집 **두 경로 모두** 연결하고 가드 테스트로 묶었다.
  - 테스트: 단위 35건 + E2E 3건(드롭세트 → DB `set_details` 저장·새로고침 유지 / 맨몸이면 잠김 /
    켜고 계속 → 무게 칸 등장·프로필 반영).

- **(5) "라이브 DB 미적용이면 적용시켜줘"** — ✅ **적용 완료**(2026-09-25 02:1x)
  - 적용 전 확인: 파일 속 `delete`/`update` 는 전부 **함수 본문 안**이라 적용 시점에 안 돈다.
    DDL 은 `create table if not exists` · `create or replace function` · `drop policy if exists`
    → **추가형·멱등**. 파일마다 트랜잭션(실패 시 그 파일만 롤백)으로 순서대로 적용.
  - 적용: `202609200002_member_share_prefs` → `202609200001_trainer_member_management`
    → `202609200003_trainer_today_prescription` → `202609220001_pet_visibility`
    → `202609220002_independent_trainers`. **5개 전부 성공.**
  - 결과: `member_share_prefs` 생성(pt_* 5개는 이미 있었음), `pt_has_pass`·`trainer_*` 함수 확인.
  - 남아 있던 실패 4건은 **가드가 낡은 것**이었다 → `216771f`
    - `trainer_board`·`trainer_member_routine`·`trainer_assign_routine_day` 는 독립 트레이너
      전환에서 **의도적으로 실행 권한을 회수**한 옛 RPC(마이그레이션 주석에 명시).
      '로그인 사용자가 부를 수 있어야 한다' → **'아무도 못 부른다'** 로 반대로 지킨다.
    - 정책 참조 함수 검사는 **익명에게 적용되는 정책만** 보도록 좁혔다. `to authenticated` 로
      못 박은 정책(pt_*)은 익명이 평가할 일이 없어 열어 주면 불필요한 문을 여는 셈(`pt_has_pass`).
  - 🟢 **`pnpm test` 220파일 / 2522테스트 green (실패 0).** tsc 0 · eslint 0.

## 🟠 다른 세션 작업 중이라 빨간 E2E (이 세션 원인 아님)

`launcher-home`·`home-layout`·`home-tab`·`redesign-navigation`·`weekly-report` 6건 실패.
원인은 **앱 격자·하단바 재작성이 진행 중**이라서다.

- 앱 격자 링크 기대 6 → 실제 5 (펫 앱이 `debugFlag: "pet"` 뒤로 숨겨짐)
- 하단바 '나' 칸 없음(`bottom-nav.tsx` 미커밋 재작성)
- 홈 `weekly-report` 카드 안 보임(홈 재작성)

스타일 치환(클래스 이름)은 링크 개수·testid 를 바꿀 수 없다. 통과 8건에는 이 세션이 구조를
바꾼 그룹원 화면 동선도 포함된다. 그쪽 작업이 끝나면 다시 돌려 볼 것.
