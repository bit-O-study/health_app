/**
 * AI 결과 안내 문구 — 로드맵 7.1(의료 조언 오해 방지).
 *
 * 🔴 왜 필요한가 — 이 앱의 AI 는 사용자의 **체중·체지방·식단**을 보고 조언한다. 그 형식이
 * 의사가 하는 말과 구분되지 않아서, 통증·질환이 있는 사람이 "AI 가 괜찮다고 했으니까" 로
 * 받아들일 수 있다. 우리는 진단할 수 없고, 진단하는 것처럼 보여서도 안 된다.
 *
 * 문구를 지킬 것 셋:
 *  1) **결과 옆에 붙인다.** 약관에만 있으면 읽는 사람이 없다 — 오해가 생기는 자리는 여기다
 *  2) **무엇을 하라고 말한다.** "책임지지 않습니다" 는 우리를 지킬 뿐 사용자를 안 지킨다.
 *     통증·질환이 있으면 전문가에게 가라고 분명히 적는다
 *  3) **결과를 가리지 않는다.** 매번 큰 경고가 뜨면 사용자는 통째로 무시하게 된다
 */
export function AiDisclaimer({ className = "" }: { className?: string }) {
  return (
    <p
      data-testid="ai-disclaimer"
      className={`text-[11px] leading-5 text-zinc-500 dark:text-zinc-400 ${className}`}
    >
      AI 가 기록을 보고 만든 <strong>참고용 조언</strong>이에요. 진단·치료가 아니며,
      통증이나 지병이 있으면 의사·전문가와 상의해 주세요.
    </p>
  );
}
